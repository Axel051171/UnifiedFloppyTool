# formats_v2/ — DEPRECATED Legacy Copies

These files are **not compiled** and are retained only for reference.
The active versions live in `src/formats/` and `src/core/`.

Do NOT add these to the build — they have basename collisions
with active sources that would cause NMAKE linker errors.

If you need to reference these, see the corresponding files in:
- src/formats/ (active parsers)
- src/core/ (active core modules)
