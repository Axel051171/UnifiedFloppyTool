# P2.4 — Release Finalisation Checklist (v4.1.4)

REFACTOR_TASKS.md **P2.4**: *"After 14 days clean: squash-merge to
`main`, tag `v4.1.4`, delete branch."*

| | |
|---|---|
| RC tag | `v4.1.4-rc1` (commit `5294563`, tagged 2026-05-14) |
| Window opens | 2026-05-14 |
| **Window closes** | **2026-05-28** — P2.4 must not start before this date |
| Source branch | n/a — `main` already carries the refactor + hardening (PR #24 + PR #26 merged) |
| Target | `main` (tag goes on HEAD; no further merge step) |
| Final tag | `v4.1.4` (no hyphen → `release.yml` ships it as a full release, not pre-release — MF-181) |

This document is the **plan**. Steps 4–8 are irreversible or affect
shared state (`main`, public tags, the remote branch) — each is marked
⚠ and **requires explicit go-ahead at execution time**. Do not
pre-authorise them.

---

## Gate 0 — Window review (precondition, on/after 2026-05-28)

P2.4 has *two* gates: the date **and** "clean". Confirm all before Step 1.

- [ ] Date is on or after **2026-05-28**.
- [ ] CI green on `refactor/type-driven-hal` for all 4 platform jobs
      (Linux GCC, Linux Qt-matrix, macOS Clang, Windows MinGW):
      `gh run list --branch refactor/type-driven-hal --limit 5`
- [ ] Sanitizer + coverage workflows green.
- [ ] No release-blocking issue filed during the RC window:
      `gh issue list --state open --label bug` — triage each.
- [ ] HIL report reviewed: `releases/v4.1.4-rc1/hil_report.md`. It is
      `NOT_RUN` by design (no rig in CI). If Axel ran HIL on real
      hardware, the signed-off `HARDWARE_TRUTH_TESTS.md` block must be
      `ALL GREEN` or the divergences explicitly accepted.
- [ ] **Audit findings are not v4.1.4 blockers — confirm this is still
      the intent.** `audit/MASTER_REPORT.md` ARCH-1/ARCH-2 (→ P1.24,
      P1.25, P1.23) are tracked **post-release** tasks; v4.1.4 ships
      with the non-GW providers unrouted, as the CHANGELOG already
      documents. ARCH-7 (task #121) is P2/P3, not a blocker. If any of
      these is now considered a 4.1.4 blocker → **STOP**, P2.4 does not
      proceed.
- [ ] Open task #121 (ARCH-7 VID/PID) acknowledged as post-release.

If any box cannot be ticked: P2.4 is **not** clean → do not proceed;
either fix-and-re-RC or extend the window.

---

## Step 1 — Version SSOT consistency

The version bump itself already happened (P2.1 / MF-173 — `VERSION.txt`
= `4.1.4`). P2.4 only **verifies** it; do not re-bump.

- [ ] `VERSION.txt` reads `4.1.4`.
- [ ] `python scripts/check_consistency.py` → `version SSOT drift : 0`.
- [ ] `include/uft/uft_version.h` is in sync (regenerated from
      `VERSION.txt` by `scripts/generate_version_header.py` at build
      time; `check_consistency.py --check version` is the gate).

```bash
cat VERSION.txt
python scripts/check_consistency.py
```

---

## Step 2 — CHANGELOG finalisation

`CHANGELOG.md` currently has `## [4.1.4-rc1] - 2026-05 (pre-release)`.
Finalise it for the real release.

- [ ] Rename the heading to `## [4.1.4] - 2026-05-28` (drop `-rc1` and
      the `(pre-release)` marker).
- [ ] Fold in anything that landed *after* the RC tag — diff:
      `git log --pretty=format:"- %s (%h)" v4.1.4-rc1..HEAD`
      (expected since the RC: MF-182…MF-188 — the P3 tester scaffolds,
      the hardware-provider audit, the Klasse-A fixes). Categorise into
      Added / Changed / Fixed / Removed.
- [ ] `RELEASE_NOTES.md` updated for v4.1.4 (incl. the
      `HARDWARE_TRUTH_TESTS.md` sign-off block once HIL is done).
- [ ] flatpak metainfo / `CLAUDE.md` header / `README.md` badge — the
      6-file list in `.claude/skills/uft-release/SKILL.md` — confirm
      each matches `4.1.4` (most were done in P2.1; re-verify).

---

## Step 3 — Pre-merge verification

- [ ] `git checkout main && git pull --ff-only` — up to date
      (refactor + hardening already merged in — see Step 4).
- [ ] Local build green (qmake is the primary build for all platforms):
      `qmake && make -j` (or the Windows MinGW equivalent).
- [ ] `ctest --output-on-failure` from the CMake test build — green
      (the 6 known-excluded tests stay excluded).
- [ ] `python scripts/check_consistency.py` → `0 / 0 / 0 / 0`.
- [ ] `python scripts/verify_build_sources.py` → no new regressions.
- [ ] `pytest tests/conformance/ tests/improvement/ tests/hil/` green
      (scaffolds: ~24 passed, the HIL/corpus populations skip — that is
      expected, P3.2–P3.4 are HIL-gated).
- [ ] Consider running the `preflight-check` agent — it simulates the
      7 known CI failure patterns locally before the tag.

---

## Step 4 — Verify `main` already contains the refactor + hardening

**Squash-merge is no longer applicable.** `main` is already equal
with `refactor/type-driven-hal` plus the 5 Phase-2 hardening commits
landed via PR #26 (MF-236 … MF-239) and the PR-#26 merge commit
itself. P2.4's original squash-merge step from REFACTOR_TASKS.md
was overtaken by events:

- PR #24 (`refactor/type-driven-hal` → `main`) was **squash-merged**
  on 2026-05-14, then closed when the refactor branch absorbed the
  external-compat audit + P3 tester scaffolds, prompting the
  re-cut RC tag on 2026-05-15 (MF-232).
- PR #26 (`tests/v4.1.5-hardening` → `main`) was merged on
  2026-05-15 with the 4 Phase-2 hardening commits + the merge
  commit. See `releases/v4.1.4/PR26_MERGE_RATIONALE.md` for why a
  PR titled "DO NOT MERGE" was nevertheless merged.

The tag therefore goes on `main` HEAD directly. Verify, do not
re-merge:

- [ ] `git fetch origin && git checkout main && git pull --ff-only`
- [ ] `git rev-list --left-right --count main...origin/refactor/type-driven-hal`
      → `N    0` with **N ≥ 5** (main is N ahead of refactor;
      refactor has zero commits main does not have). If the right
      side is non-zero — STOP, a refactor commit slipped in that
      is not on main.
- [ ] `git log --oneline -1 main` → `21ff31b` (or later — verify the
      MF-NNN footer matches expected scope).
- [ ] `git diff main origin/refactor/type-driven-hal -- src/ include/`
      is empty OR only the expected hardening-test deltas (which
      under `src/` / `include/` should be **empty** — the hardening
      branch only added `tests/`).
- [ ] `git diff --stat main origin/tests/v4.1.5-hardening` → empty
      (the hardening branch is fully merged into main).
- [ ] PR #26 (merge commit `21ff31b5`) carries a `git notes`
      explanation of why a PR titled "DO NOT MERGE" was nevertheless
      merged (MF-242 — see `releases/v4.1.4/PR26_MERGE_RATIONALE.md`
      for the long form). Verify: `git fetch origin
      refs/notes/commits:refs/notes/commits && git notes show
      21ff31b5` — must return the MF-242 explanation.
- [ ] No further merge / squash needed. Proceed to Step 5 (tag).

---

## Step 5 — ⚠ Tag `v4.1.4`

**Public tag — triggers the release pipeline once pushed.**

- [ ] Create a **signed** tag (forensic software ships signed tags;
      note the RC tag `v4.1.4-rc1` was *unsigned* — if no GPG key is
      configured, decide explicitly whether to configure one now or
      tag unsigned, and record the decision here):
      `git tag -s v4.1.4 -m "UFT v4.1.4 — type-driven HAL refactor"`
- [ ] `git tag -v v4.1.4` — verify (or `git show v4.1.4` if unsigned by
      decision).
- [ ] The tag has **no hyphen** → `release.yml`'s
      `prerelease: ${{ contains(github.ref_name, '-') }}` evaluates
      false → GitHub marks it a full release / "Latest" (MF-181).

---

## Step 6 — ⚠ Push `main` + the tag

**This is the point of no return — public release machinery runs.**

- [ ] `git push origin main`
- [ ] `git push origin v4.1.4`
- [ ] Watch the release workflow: `gh run watch` /
      `gh run list --workflow=release --limit 1` — must reach
      `completed / success` on all platform jobs before Step 7.
- [ ] If the release build fails after the tag push: the version number
      is *burned*. Do **not** force-move the tag — fix forward
      (`v4.1.4` patch follow-up or `v4.1.5`). Escalate before any
      tag-rewrite.

---

## Step 7 — Verify the GitHub release

- [ ] `gh release view v4.1.4` — exists, **not** marked pre-release,
      shows as "Latest".
- [ ] Artefacts attached: `.deb`, `.tar.gz` (Linux), `.dmg` (macOS),
      `.tar.gz` (Windows), `SHA256SUMS` — same set as v4.1.0.
- [ ] Release notes drafted from the finalised `CHANGELOG.md` [4.1.4]
      section.
- [ ] `releases/v4.1.4/` — add the final `hil_report.md` (re-run
      `python tests/hil/run_hil.py --version v4.1.4` or copy the
      signed-off HIL result) so the release dir mirrors the RC dir.

---

## Step 8 — ⚠ Delete the refactor + hardening branches

**Only after Step 7 confirms the release is good.**

Two branches to retire — `refactor/type-driven-hal` (the V1→V2
refactor, content already on `main` via PR #24) and
`tests/v4.1.5-hardening` (Phase-2, content already on `main` via
PR #26).

- [ ] Confirm `main` contains both branches' content:
      `git diff --stat main origin/refactor/type-driven-hal` → empty.
      `git diff --stat main origin/tests/v4.1.5-hardening`    → empty.
- [ ] `git branch -D refactor/type-driven-hal` (local; `-D` because
      squash-merge breaks ancestry — only after the `diff --stat`
      above is empty).
- [ ] `git branch -D tests/v4.1.5-hardening` (same — squash-merge
      ancestry caveat).
- [ ] `git push origin --delete refactor/type-driven-hal` (remote).
- [ ] `git push origin --delete tests/v4.1.5-hardening` (remote).
- [ ] Confirm no open PR still targets either branch.

> Note on `-d` vs `-D` with squash-merge: a squash-merge does **not**
> make `main` an ancestor of the branch tip, so `git branch -d` will
> refuse. Use `-D` only after the `diff --stat` above is empty —
> that is the authoritative ancestry-replacement check.

---

## Step 9 — Post-release housekeeping

- [ ] REFACTOR_TASKS.md: P2.3 → ✅, P2.4 → ✅ (with the v4.1.4 tag +
      merge commit hash).
- [ ] Task #106 (P2.3) and the P2.4 task → completed.
- [ ] The post-release backlog stays open and is **not** part of
      v4.1.4: P1.20–P1.25 (raw_handle removal, non-GW routing, ARCH-2
      flux decode, ARCH-3 UFI backend), P3.2–P3.4 (HIL test
      populations), task #113 (FluxEngine refinements), task #121
      (ARCH-7 VID/PID). Decide their target — likely a v4.1.5 / v4.2
      milestone.
- [ ] Branch protection / default branch unchanged (`main`).

---

## Rollback / abort

- **Before Step 6 (push):** everything is local — `git reset`, re-do,
  or abandon. No public impact.
- **After Step 6:** the tag and release are public. Do **not** delete
  or move a published tag. Roll *forward* with `v4.1.5`. The only
  exception — a release so broken it must be pulled — is a deliberate,
  separately-authorised action, not part of P2.4.

## Reproduce / dry-run before the real run

```bash
# safe, read-only — run any time to pre-check Gate 0 + Steps 1–3:
gh run list --branch refactor/type-driven-hal --limit 5
cat VERSION.txt && python scripts/check_consistency.py
python scripts/verify_build_sources.py
git log --pretty=format:"- %s (%h)" v4.1.4-rc1..HEAD
git diff --stat main refactor/type-driven-hal | tail -1
```
