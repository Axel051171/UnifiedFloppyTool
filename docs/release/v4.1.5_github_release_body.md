# UFT v4.1.5 — Hardware Hardening + Tier-2.5 Simulator System

**Release-Body-Draft für Phase B.5** (`V415_DEMO_READY_GOAL.md`).
**Copy-paste fertig** für `gh release create v4.1.5 --notes-file <this-file>`
am Tag-Day (2026-05-29+).

---

UnifiedFloppyTool v4.1.5 hardens v4.1.4 across 9 dimensions without
breaking any user-facing behavior. **Greaseweazle workflow is unchanged.**

## Highlights

- **9 V2 hardware providers** with live code paths (1 production + 8 honest scaffolds)
- **Tier-2.5 simulator system** — 10/10 SIMULATED coverage without owning a single Floppy controller
- **`libusb-mock` framework** — byte-protocol-validated SCP-Direct + XUM1541 wire-tests in CI
- **LOSS.preflight** wired to all 44 conversion paths at the `uft_convert_file()` chokepoint
- **22/22 SCP USB opcodes** byte-exact verified against the samdisk reference implementation
- **Plugin Prinzip-7 compliance:** 80/80 with `spec_status` + `features` populated (was 15/80 + 5/80)
- **Plugin ABI gate:** `api_version` field + `_Static_assert(sizeof == 216)` + registrar reject for future-ABI plugins

## What changed

For the full change log see [`RELEASE_NOTES.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/RELEASE_NOTES.md)
v4.1.5 section. Capability matrix:
[`docs/CAPABILITIES.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/CAPABILITIES.md).

### Resolved structural issues

| ID | Resolution |
|---|---|
| UFT-001 | 9/9 V2-Provider have live code paths (MF-249..MF-258) |
| UFT-002 | CMakeLists.txt version comment removed, refers to VERSION.txt SSOT |
| UFT-003 | HardwareTab honest-stub styled distinctly (orange "Preview") |
| UFT-004 | `uft_format_plugin_t` ABI gate (MF-260) |
| UFT-005 | `test_transitions_ns_contract` extended with KryoFlux + FluxEngine FFI shields |
| UFT-006 | `.claude/CLAUDE.md` 6 → 9 V2-provider list updated |
| UFT-007 | VID/PID SSOT in `uft_scp_direct.h` confirmed (MF-212) |
| ARCH-2 | KryoFlux/FluxEngine container-byte fabrication closed (MF-208/MF-209) |
| SCP-D1 | SCP USB opcodes verified vs samdisk `SuperCardPro.h` (MF-261) |

## Hardware Compatibility

See [`docs/CAPABILITIES.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/CAPABILITIES.md)
for the full per-controller capability matrix (Tier-3 / Tier-2.5 / mock-only / scaffold).

Status quick-glance:
- **1 production** (Tier-3 PASS): Greaseweazle (read+write)
- **3 read-real via subprocess wrapper:** KryoFlux (DTC), FluxEngine (CLI), FC5025 (fcimage)
- **3 libusb-wired, mock-only:** SCP-Direct, XUM1541, Applesauce (byte-protocol validated)
- **1 USB-CDC-wired with sim:** ADF-Copy
- **1 Linux-only:** USB-Floppy (SG_IO; Win/Mac is v4.1.6)

Any provider not in Tier-3 PASS reports `not_implemented` for unwired calls
— **never a silent no-op.** GUI shows orange "Preview" badge instead of
the green Production badge.

## What this release does NOT ship (deferred to v4.1.6)

- M3.2 XUM1541 real-HW Tier-3 verification (needs opencbm + real device)
- M3.3 Applesauce `?disk` read state-machine completion
- UFI Windows + macOS backends (Linux SG_IO is live)
- ARCH-9 XUM1541 macOS .dylib loader
- 5-and-3 Apple GCR (DOS 3.2 13-sector)
- FM-Decoder completion (`flux_decode_fm` flagged incomplete)
- Per-track exact loss counts (current Phase 2 is category-level)
- `uft-decode` CLI build wiring (scaffold ships, CMake/qmake enable v4.1.6)
- MF-271 Switch/cart7 delete (decided 2026-05-25, executes post-tag)

## Upgrade notes

- **No breaking changes.** v4.1.4 → v4.1.5 is purely additive.
- The `uft_format_plugin_t` ABI gate (MF-260) is opt-in: legacy plugins
  with `api_version=0` are accepted with a one-shot stderr warning.
  Plugin authors should set `.api_version = UFT_PLUGIN_API_VERSION`
  before v5.0.
- LOSS.preflight requires explicit consent for `LOSSY_DOCUMENTED`
  conversions. GUI workflows that previously ran lossy converts
  silently now need to set `accept_data_loss=true` in
  `uft_convert_options_t` (`preserve_errors` field maps to this).

## Verified on (CI green)

- Linux GCC (Qt 6.7.3 c++17 + Qt 6.10.1 c++20)
- macOS Clang
- Windows MinGW-w64 (Qt 6.10.1)
- Plugin compliance: 80/80 ✓
- Build parity: A=216 / B=0
- Consistency: 0/0/0/0 ✓
- Test suite: 153/153 (100%)
- Tier-2.5 simulator gate: 10/10 SIMULATED
- Sanitizer (ASan + UBSan): green

## Demo + Documentation

- 1-page pitch: [`docs/SHOWCASE.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/SHOWCASE.md)
- Capability matrix: [`docs/CAPABILITIES.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/CAPABILITIES.md)
- 10-min hands-on demo: [`docs/demo/QUICK_DEMO.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/demo/QUICK_DEMO.md)
- Known limits: [`docs/KNOWN_ISSUES.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/KNOWN_ISSUES.md)
- Design principles: [`docs/DESIGN_PRINCIPLES.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/DESIGN_PRINCIPLES.md)

## Artifacts

- `UnifiedFloppyTool_4.1.5_amd64.deb` (Ubuntu 22.04+ / Debian 12+)
- `UnifiedFloppyTool-4.1.5-linux-x86_64.tar.gz` (portable Linux)
- `UnifiedFloppyTool-4.1.5.dmg` (macOS Universal)
- `UnifiedFloppyTool-4.1.5-windows-x86_64.zip` (Windows portable, MinGW)
- `SHA256SUMS` + `SHA256SUMS.asc` (signed)

## Bug reports & feedback

GitHub Issues. Please check
[`KNOWN_ISSUES.md`](https://github.com/Axel051171/UnifiedFloppyTool/blob/v4.1.5/docs/KNOWN_ISSUES.md)
first — many limits are intentional v4.1.6 scope-deferrals.

UFT follows a hard conflict hierarchy: **Forensics beats performance.
Forensics beats UX. Security beats compatibility.** Feature requests
that contradict these rules will be declined with reasoning.

---

## Tag-Day Promotion Notes (not in published Release Body)

This file lives at `docs/release/v4.1.5_github_release_body.md` as a
copy-paste source. To publish the actual GitHub Release at Phase B.5:

```bash
# After Phase B.3 (tag created + pushed) and release.yml CI artifacts ready:
gh release create v4.1.5 \
    --title "UFT v4.1.5 — Hardware Hardening + Tier-2.5 Simulator System" \
    --notes-file docs/release/v4.1.5_github_release_body.md \
    --target main \
    dist/UnifiedFloppyTool_4.1.5_amd64.deb \
    dist/UnifiedFloppyTool-4.1.5-linux-x86_64.tar.gz \
    dist/UnifiedFloppyTool-4.1.5.dmg \
    dist/UnifiedFloppyTool-4.1.5-windows-x86_64.zip \
    dist/SHA256SUMS \
    dist/SHA256SUMS.asc
```

Strip everything below the `---` separator before publishing (the
"Tag-Day Promotion Notes" section is dev-internal).
