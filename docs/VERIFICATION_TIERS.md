# Format-Verifikations-Stufen (generiert)

**NICHT von Hand editieren** — erzeugt von `scripts/gen_verification_tiers.py` (MF-364). Definitionen: [`VERIFICATION_PLAN.md`](VERIFICATION_PLAN.md).

Ein T3 mit Test-Eintrag bedeutet: es existiert ein synthetischer Test, aber die Byte-Struktur wurde nie gegen eine autoritative externe Quelle verifiziert — genau die Konstellation, in der die fabrizierten Parser (FMT-2/3/10/11/12) gruen waren.

## Zusammenfassung

| Stufe | Formate |
|---|---|
| T1 | 2 |
| T1b | 36 |
| T2 | 29 |
| T3 | 21 |
| **gesamt** | **88** |

## Pro Format

| Plugin | Stufe | Tests | Spec-Quelle | Evidenz | Korpus-Images |
|---|---|---|---|---|---|
| `fdi` | **T1** | `test_corpus_fdi`, `test_fdi_spectrum`, `test_format_probe_fuzz`, `test_plugin_probe_real` | SAMdisk ReadFDI (src/samdisk/fdi.cpp, in-tree) + WoS format FAQ | MF-359 | 1 |
| `g64` | **T1** | `test_c64_metrics_corpus`, `test_c64_protection_real_corpus`, `test_convert_via_plugin`, `test_d64_42_spuren_rundlauf`, `test_d64_writer`, `test_format_probe_fuzz`, `test_g64_speedzonen`, `test_g64_v3_halbspur_index`, `test_plugin_probe_real`, `test_sector_id_on_disk` | — | — | 3 |
| `adf` | **T1b** | `test_adf_teilabzug`, `test_adf_write_roundtrip`, `test_corpus_adf`, `test_format_probe_fuzz`, `test_plugin_probe_real`, `test_sector_id_on_disk` | — | — | 1 |
| `atr` | **T1b** | `test_atr_512`, `test_atr_boot_layout`, `test_atr_write_roundtrip`, `test_corpus_atr`, `test_corpus_xfd`, `test_format_probe_fuzz`, `test_plugin_probe_real` | — | — | 1 |
| `d64` | **T1b** | `test_convert_via_plugin`, `test_corpus_d64`, `test_d64_42_spuren`, `test_d64_42_spuren_rundlauf`, `test_d64_42track`, `test_d64_errormap`, `test_d64_geometry_zones`, `test_d64_schreiber_symmetrie`, `test_d64_write_roundtrip`, `test_disk_analyzer_no_fiction`, `test_disk_open_fuzz`, `test_format_probe_fuzz`, `test_g64_v3_halbspur_index`, `test_plugin_identity`, `test_plugin_probe_real`, `test_sector_id_on_disk`, `test_smart_open_quality` | VICE D64 sizes incl. error-block trailer + 40/42-track variants | MF-333, MF-350 | 1 |
| `d67` | **T1b** | `test_convert_via_plugin`, `test_corpus_cbm_vice`, `test_d64_42_spuren_rundlauf`, `test_plugin_identity`, `test_smart_open_quality` | VICE/Schepers D67 690-block geometry (176640 bytes) | MF-314 | 1 |
| `d71` | **T1b** | `test_corpus_d71`, `test_d71_write_roundtrip`, `test_format_probe_fuzz`, `test_plugin_probe_real` | — | — | 1 |
| `d80` | **T1b** | `test_corpus_cbm_vice`, `test_d80_write_roundtrip` | — | — | 1 |
| `d81` | **T1b** | `test_corpus_d81`, `test_d81_write_roundtrip`, `test_format_probe_fuzz`, `test_plugin_identity`, `test_plugin_probe_real`, `test_smart_open_quality` | — | — | 1 |
| `d82` | **T1b** | `test_corpus_cbm_vice`, `test_d82_write_roundtrip` | — | — | 1 |
| `d88` | **T1b** | `test_d88_error_marks`, `test_d88_header_variants`, `test_format_probe_fuzz`, `test_fremde_hand_liest`, `test_plugin_probe_real` | pc98.org D88 + MAME d88_dsk (DDAM @+07, FDC status @+08) | MF-336 | 1 |
| `dim_atari` | **T1b** | `test_dim_atari_magic` | — | — | 1 |
| `dmk` | **T1b** | `test_dmk_crc`, `test_dmk_rx02_absage`, `test_fremde_hand_liest` | David Keil DMK spec (openMSX DMK-Format-Details) + WD177x CRC-CCITT pinned to check value 0x29B1 | MF-353 | 1 |
| `dsk_cpc` | **T1b** | `test_edsk_error_marks`, `test_format_probe_fuzz`, `test_fremde_hand_liest`, `test_plugin_probe_real` | EDSK uPD765 ST1/ST2 status-bit semantics (bit5 CRC, ST2 bit6 deleted); MF-332 verified the dsk_cpc implementation (the separate 'edsk' plugin in amstrad/ remains untested) | MF-332 | 1 |
| `edsk` | **T1b** | `test_format_probe_fuzz`, `test_korpus_container`, `test_plugin_probe_real` | — | — | 1 |
| `g71` | **T1b** | `test_corpus_cbm_vice`, `test_g71_read` | — | — | 1 |
| `hfe` | **T1b** | `test_convert_hfe_adf`, `test_corpus_hfe`, `test_format_probe_fuzz`, `test_hfe_interface_modes`, `test_hfe_track0_encoding`, `test_hfe_v3_weak`, `test_hfe_write_allowed`, `test_plugin_probe_real` | HxC hfev3_loader.c opcode semantics (v3 decode + RAND weak bits); HxC HFE docs (v1); MF-897 Spur-0-Ersatz: HxC hfe_loader.c + hfe_writer.c, gegengelesen an src/samdisk/hfe.cpp (track0s0_altencoding), gemessen an tests/corpus_free/gw_amigados.hfe; MF-898 write_allowed: src/samdisk/hfe.cpp + Feldname, gemessen an derselben Datei | MF-354, MF-362, MF-897, MF-898 | 1 |
| `imd` | **T1b** | `test_format_probe_fuzz`, `test_fremde_hand_liest`, `test_imd_error_marks`, `test_imd_track_record`, `test_imd_write_roundtrip`, `test_plugin_probe_real` | MAME src/lib/formats/imd_dsk.cpp (sector numbering map unconditional: "offs += 5 + sector_num"; cylinder map if header[2] & 0x80; head map if & 0x40) cross-checked against hharte/libimd src/libimd.h (IMD_HFLAG_CMAP_PRES 0x80, IMD_HFLAG_HMAP_PRES 0x40, LIBIMD_MAX_SECTORS_PER_TRACK 256) | MF-430 | 1 |
| `img` | **T1b** | `test_corpus_gw_geometrie`, `test_format_probe_fuzz`, `test_img_write_roundtrip`, `test_plugin_probe_real`, `test_sector_id_on_disk` | — | — | 1 |
| `jv3` | **T1b** | `test_fremde_hand_liest`, `test_jv3_gegen_mame` | MAME `formats/trs80_dsk.cpp` (BSD-3-Clause, Dirk Best), in `neue-ideen/formats.zip` — `jv3_format::identify()` gibt Kopfgroesse, Satzarithmetik, Groessenregel und jede Pruefung woertlich; unabhaengig bestaetigt durch Tim Mann, „Common File Formats for Emulated TRS-80 Floppy Disks“ (https://www.tim-mann.org/trs80/dskspec.html, abgerufen 2026-09-10), der die XOR-1/XOR-2-Regel und die drei Flag-Felder ebenfalls woertlich nennt | MF-1017 — fuenf Befunde, und die ersten zwei zusammen bedeuteten, dass **jeder Sektor jeder JV3-Datei falsche Bytes lieferte**, still, mit `UFT_OK`. Gemessen mit einem Wegwerf-Programm an einer nach MAMEs Arithmetik gebauten Datei — drei Sektoren (Fuellbytes A0, B1, D2) mit einem freien Satz dazwischen (CC):

    Spur 0/0   : 2 Sektoren   (Orakel: 3)
       Sektor 1: Fuellbyte B1     <- gehoert Sektor 1, nicht Sektor 0
       Sektor 1: Fuellbyte CC     <- der FREIE Datenraum

**(1) Der Datenanfang lag 256 Byte zu weit.** `JV3_HEADER_SIZE` war `0x2300`, mit dem Kommentar „dir + writeprot + padding“, und der Dateikopf setzte das Schreibschutz-Byte auf 0x2200. Wirklich ist es das **letzte** Byte des Kopfbereichs (0x21FF), und `2901*3 + 1` ergibt **0x2200** — keine Polsterung. MAME woertlich: `const uint32_t header_size = entries * 3 + 1;`, und sein `identify()` prueft das Schreibschutz-Byte bei `data[0x21ff]`. Klasse MF-796 (`edsk` meldete „9 Sektoren“ und lieferte keinen) und MF-794 (`sad` las 158 von 160 Spuren an der falschen Stelle). **(2) Der Verzeichnislauf brach beim ersten freien Satz ab.** MAMEs Beschreibung sagt ausdruecklich das Gegenteil: „Unused descriptors are FF FF (FC | size). **These can be intermixed with valid descriptors.**“ Sein `identify()` laeuft alle 2901 Saetze ab, ueberspringt die freien und schiebt den Datenzeiger um deren Groesse weiter — freie Saetze belegen Datenraum, und ihre Groesse verrechnet sich mit **2** statt mit 1. Der alte Lauf verlor alles hinter der ersten Luecke. **(3) Der Freimarker ist die SPUR allein.** Geprueft wurde `trk == 0xFF && sid == 0xFF`; MAME prueft `if (track < 0xff)`. Ein Satz mit Spur 0xFF und einer Sektornummer ungleich 0xFF galt als benutzt und lieferte einen Sektor der Spur 255. **(4) Die Sektornummern kippten zusammen.** `uint8_t sec_num = e->sector_id; if (sec_num > 0) sec_num--;` vor einem `uft_format_add_sector()`, das 1 addiert — die Schranke `> 0` machte die Gegenrechnung fuer Sektor **0** unwirksam: 0 blieb 0, plus 1 ergab **1**. Sektor 0 und Sektor 1 landeten beide auf ID 1, genau wie in der Messung oben. **(5) Dichte und DAM standen in falschen Bits.** Der Dateikopf sagte „bit 5 = double density, bit 6 = deleted address mark“; MAME und Tim Mann sagen **Bit 7 = Dichte** und **Bits 5-6 = DAM-Kode** (0xF8 deleted = 0x60 in Einzel-, 0x20 in Doppeldichte; 0xF9 = 0x40; 0xFB normal = 0x00). Der Code las `flags & 0x40` als „deleted“ — das trifft in Einzeldichte die **0xF9** und in Doppeldichte einen ungueltigen Wert; die Dichte wurde **nie** gelesen. **Und ein zweiter gruener Test hat einen Defekt bewacht:** `tests/test_register_all_formats.c` schrieb `0x2300` als Kopfgroesse fest (`const size_t total = 0x2300 + 20 * 256;`). Nach der Reparatur fiel er sofort. Das ist der **zweite Fall in derselben Runde** nach `test_plugin_probe_real.c`/MF-1016 — die Tests, die je ein Loch geschlossen haben, hielten das Nachbarloch fuer richtig. Gestalt von MF-992. **Zusaetzlich uebernommen, was das Orakel wirklich prueft:** Schreibschutz-Byte 0x00/0xFF, DMK-Unterscheidung (Bytes 5..15 alle null), Spur < 96, Sektor < 19, keine Doppelvergabe von (Seite, Spur, Sektor), `last_data <= Dateigroesse`, und das non-IBM-Bit wird **benannt abgewiesen** (MAME: „it's impossible to support it“) statt geraten. Ein zweiter Satzblock wird ebenfalls nicht angenommen — MAME: „no TRS-80 disks need this extra area, so it's unsupported“. **Mutationsmatrix 6 von 6**: Datenanfang zurueck auf 0x2300 (faellt 5) · beim ersten freien Satz abbrechen (3) · Freimarker wieder Spur UND Sektor (**genau 1**) · Sektornummer wieder dekrementieren (3) · „deleted“ wieder als Bit 6 allein (**genau 1**, und die Meldung zeigt woertlich `0x40=1`) · freie Saetze mit XOR 1 statt XOR 2 verrechnen (2, und Sektor 2 bekommt die Bytes des freien Blocks). **T2 und nicht T1b:** kein JV3 von fremder Hand im Korpus, kein Orakel-Binary verfuegbar. Die Pruefdatei ist nach der Arithmetik des Orakels gebaut — nicht nach UFTs Code — und der freie Satz ist mit Absicht **512** Byte gross (flags&3 = 0, XOR 2), damit die XOR-2-Regel wirklich geprueft ist: mit XOR 1 waeren es 256, und der Sektor dahinter laege falsch. | 1 |
| `jvc` | **T1b** | `test_corpus_gw_geometrie`, `test_schranken_die_nie_greifen` | — | — | 1 |
| `kfx` | **T1b** | `test_apple_moof_a2r_no_door`, `test_fremde_hand_liest`, `test_kfx_probe_overclaims`, `test_kfx_sonde_sagt_nein` | — | — | 2 |
| `micropolis` | **T1b** | `test_corpus_gw_geometrie` | — | — | 1 |
| `msa` | **T1b** | `test_format_probe_fuzz`, `test_korpus_container`, `test_msa`, `test_plugin_probe_real` | SAMdisk 4.0 (MIT), src/samdisk/msa.cpp:9-16 MSA_HEADER + :38-44 Plausibilitaet | MF-460 — 10-Byte-Kopf Feld fuer Feld verglichen, alle big-endian: Magic 0x0E0F, Sektoren/Spur, Seiten MINUS EINS, Startspur, Endspur. Der +1-Aufschlag bei den Seiten stand nicht in unserer Beschreibung, wird vom Code aber richtig gerechnet (wie samdisk/msa.cpp:44). Die Probe pruefte nur das Magic und meldete 95; sie prueft jetzt zusaetzlich, was msa_plugin_open() ohnehin verlangt, plus SAMdisks Nullbyte-Pruefung der oberen Feldbytes. | 2 |
| `msx_disk` | **T1b** | `test_corpus_msx` | — | — | 1 |
| `northstar` | **T1b** | `test_corpus_gw_geometrie` | — | — | 1 |
| `pdp` | **T1b** | `test_corpus_gw_geometrie` | — | — | 1 |
| `po` | **T1b** | `test_apple_do_po_bounds`, `test_corpus_gw_geometrie`, `test_do_po_probe_ignores_content`, `test_durchschreibprobe`, `test_format_probe_fuzz`, `test_plugin_probe_real`, `test_po_write_roundtrip` | — | — | 1 |
| `sad` | **T1b** | `test_sad_magic` | — | — | 1 |
| `sam` | **T1b** | `test_corpus_gw_geometrie` | — | — | 1 |
| `sap_thomson` | **T1b** | `test_fremde_hand_liest` | — | — | 1 |
| `scp` | **T1b** | `test_convert_scp_adf`, `test_corpus_scp`, `test_flux_jitter_vectors`, `test_format_probe_fuzz`, `test_g64_v3_halbspur_index`, `test_plugin_probe_real`, `test_protection_probe`, `test_scp_footer_roundtrip`, `test_scp_layout`, `test_scp_readers_agree`, `test_scp_weakbit_multirev`, `test_scp_writer_roundtrip` | cbmstuff SCP image spec (48-byte FPCS footer, bitcell track length) | MF-318, MF-351 | 1 |
| `ssd` | **T1b** | `test_corpus_gw_geometrie`, `test_ssd_hadfs_nicht_dfs` | — | — | 1 |
| `st` | **T1b** | `test_fremde_hand_liest`, `test_st_bootpruefsumme`, `test_st_geometry`, `test_st_spiralfaktor`, `test_st_write_roundtrip` | SAMdisk 4.0 (MIT), src/samdisk/st.cpp:6-67 (Boot-Pruefsumme, BPB-Pfad, Geometrie-Scan) + src/samdisk/bpb.h (BIOS Parameter Block). BPB-Offsets unabhaengig gegengelesen an der DOS-Bootsektor-Spalte der CopyQM-Layoutbeschreibung (rio.early8bitz.de, siehe Eintrag cqm), die dieselben Offsets 0x0b/0x18/0x1a nennt. MF-873: die TOS-Boot-Pruefsumme haengt nicht mehr an SAMdisk allein. Zweite Quelle disktype, Dokumentation §3.3.1 "The GEMDOS File System" (https://disktype.sourceforge.net/doc/ch03s03.html): "the boot sector must have a checksum of $1234 (computed from 16-bit words in big-endian byte order)" — benennt Wert, Wortbreite UND Byte-Reihenfolge. Dritte Quelle Richard Karsmakers, "The Ultimate Virus Killer Book", Anhang I "Atari Boot Flowchart", Schritt 9 (https://st-news.com/uvk-book/the-book/part-iii-appendices/i-atari-boot-flowchart): "the Operating System will load the bootsector off that particular floppy disk and execute it when the checksum is $1234" — benennt Wert und Folge, nicht die Mechanik. | MF-462 — ST ist kopflos, die Geometrie muss also aus BPB oder Dateigroesse kommen; verifiziert wurde die Reihenfolge und der Umfang beider Wege. (a) BPB zuerst: Bytes/Sektor 0x0B, Gesamtsektoren 0x13, Sektoren/Spur 0x18, Koepfe 0x1A, akzeptiert nur wenn in sich stimmig UND die Dateigroesse exakt erklaert — wie samdisk/st.cpp:24-45. Das entscheidet den Fall 368.640 Byte, der 80x1x9 UND 40x2x9 ist; der Dateikopf nannte beide Lesarten, der Code nahm still die erste. (b) Groessen-Scan als Rueckfall ueber 80..84 Zylinder und 8..11 Sektoren wie samdisk/st.cpp:47-67 — die erweiterten ST-Formate mit 82/83 Spuren und 10/11 Sektoren wurden vorher AUSNAHMSLOS abgewiesen. Die Reihenfolge des Scans ist so gewaehlt, dass alle sechs bisher bekannten Groessen unveraendert aufgeloest werden (Test the_six_legacy_sizes_resolve_unchanged). Zusaetzlich uebernommen: die TOS-Boot-Pruefsumme (256 Big-Endian-Woerter des Bootsektors summieren zu 0x1234, samdisk/st.cpp:6) als einziges positives Erkennungsmerkmal des Formats. MF-873: seit diesem Stand DREIFACH belegt (disktype §3.3.1 und das UVK-Buch, siehe spec_source); disktype spricht als einzige die Byte-Reihenfolge aus, was die Bedingung "nicht doppelt gegengelesen" aufhebt. Dazu tests/test_st_bootpruefsumme.c mit einer GEGENSPUR: ein Bootsektor, dessen LITTLE-Endian-Summe 0x1234 trifft und dessen Big-Endian-Summe nicht — er unterscheidet sich vom gueltigen ausschliesslich im Ausgleichswort an $1FE. Zwei Mutationen gemessen: (1) Pruefer little-endian — test_st_geometry wird ebenfalls rot, der neue Test fuegt hier NICHTS hinzu; (2) Pruefer nimmt BE ODER LE — test_st_geometry bleibt GRUEN, nur der neue Test faengt es. Nur der zweite Fall rechtfertigt die Datei, und das steht so in ihrem Kopf. NICHT verifiziert: Verhalten an einem realen ST-Abbild (keines im Korpus) — daher T2. | 1 |
| `stx` | **T1b** | `test_format_probe_fuzz`, `test_fremde_hand_liest`, `test_plugin_probe_real`, `test_stx_error_marks`, `test_stx_registrierter_leser` | Pasti STX descriptor spec (atari.8bitchip.info/STXdesc) | MF-335 | 1 |
| `t1k` | **T1b** | `test_corpus_gw_geometrie` | — | — | 1 |
| `trd` | **T1b** | `test_corpus_gw_geometrie`, `test_kurze_datei_erfindet_keine_sektoren` | — | — | 1 |
| `xfd` | **T1b** | `test_corpus_xfd`, `test_format_probe_fuzz`, `test_plugin_identity`, `test_plugin_probe_real`, `test_smart_open_quality` | — | — | 1 |
| `86f` | **T2** | `test_86f_spec_conformance` | 86Box/docs, dev/formats/86f.rst — die Beschreibung des Formats durch dessen URHEBER (abgerufen 2026-09-08). Zweite, unabhaengige Quelle: Digitoxin1/DiskImageTool, ImageFormats/86F/86FImage.vb, ein eigenstaendiger Leser mit demselben Magic und derselben Kopfgroesse (belegt MF-708). KEINE Zeile fremden QUELLCODES gelesen — weder 86Box' fdd_86f.c noch fluxfox' Rust-Leser; der Kanal ist die Spezifikation (MF-695). | MF-961 — der Leser ist gegen die Spezifikation neu gefasst und an einer ECHTEN Datei abgenommen. Vorzustand: das Plugin probte auf "86BX", ein Magic, das in keiner 86F-Datei steht, nahm einen 32-Byte-Kopf an und erfand eine 12-Byte-Spurtabelle mit offset/length/flags/sectors/rpm — es wies damit JEDE echte Datei ab und meldete dabei Read/Flux als SUPPORTED (MF-707/708). Neu gefasst: Magic "86BF", 8-Byte-Kopf, 32-Bit-Spur-Offsets je Seite je Spur, Spurkopf mit Zellzahl (Disk-Flags Bit 7 und 12), MFM-Bitstrom durch den geprueften uft_mfm_decode_track(). An der Datei nachgemessen, BEVOR eine Zeile geschrieben wurde: Tabelle 512 Eintraege, 172 belegt (86 Zyl x 2 Seiten), Spur-Flags 0x000A = MFM/250 kbps, Ende der letzten Spur == Dateigroesse (Differenz 0), Bitstrom MSB zuerst mit 54 Treffern 0x4489 je Spur = 9 Sektoren. Abnahme: 7 von 7 Spuren, 63 von 63 Sektoren byteweise gegen eine unabhaengig nachgerechnete FORMEL (Sektor k = 512 x Byte (k mod 256), 0 von 720 Abweichungen an der beiliegenden .img gemessen). NICHT verifiziert: Schreiben (write_track sagt ab), FM/M2FM/GCR-Spuren (kein Bitstrom-Dekoder), die Oberflaechenbeschreibung (schwache Bits). T2 und nicht T1b, weil der Erzeuger der .86f nach Messung des Scouts sehr wahrscheinlich fluxfox selbst ist und nicht das KANONISCHE 86Box — eine fremde Hand, aber nicht die richtige. | — |
| `adf_arc` | **T2** | `test_acorn_adfs_identity` | DiscImageManager (geraldholdsworth), als Spec gelesen: die drei verbliebenen Groessen sind gegen DiscImage_ADFS.pas:73-79 und die mitgelieferten Leer-Abbilder geprueft — ADFS_D.adf/ADFS_E.adf = 819200 (80x2x5x1024), ADFS_F.adf = 1638400 (80x2x10x1024), 327680 = ADFS M einseitig (80x1x16x256). 655360 wurde mit MF-654 ENTFERNT: es ist ADFS L, gehoert uft_adl.c und liegt spurverschraenkt, was dieser lineare Leser falsch getroffen haette. Geprueft wurden Groessen und Geometrien — NICHT die Verzeichnisstruktur. | MF-654 | — |
| `adf_ext` | **T2** | `test_adf_ext_plugin` | WinUAE disk.cpp read_header_ext2 (UAE-1ADF) | MF-352 | — |
| `adl` | **T2** | `test_acorn_adfs_identity` | DiscImageManager (geraldholdsworth), als Spec gelesen: DiscImage_Private.pas:188 bindet .adl an ADFS L; DiscImage_ADFS.pas:73-75 gibt 163840/327680/655360 = S/M/L; DiscImage_Private.pas:547-570 gibt die Seitenverschraenkung fuer ADFS L (FInterleave=2): offset = (cyl*2+head)*4096. Gegenprobe am mitgelieferten ADFS_L.adl = 655360 Byte, selbst gemessen. Geprueft wurden Groesse, Seitenzahl und Spurablage — NICHT die Verzeichnisstruktur. | MF-654 | — |
| `akai_s900` | **T2** | `test_akai_s900_plugin` | akaiutil (kmi9000) geometry: DD 5x1024/819200, HD 10x1024/1638400 | MF-348 | — |
| `apridisk` | **T2** | `test_apridisk_gegen_mame`, `test_apridisk_schreibt_in_die_datei` | MAME, `src/lib/formats/apridisk.cpp` und `.h` — `apridisk_format::identify()`/`::load()`, `enum { APR_DELETED, APR_SECTOR, APR_COMMENT, APR_CREATOR }`, `APR_UNCOMPRESSED = 0x9e90`, `APR_COMPRESSED = 0x3e5a`, `APR_HEADER_SIZE = 128`, `SECTOR_SIZE = 512`. **BSD-3-Clause**, Dirk Best. Vom Eigentuemer als `neue-ideen/formats.zip` beigesteuert; Herkunftsentscheidung MF-1003. Kein MAME-Code uebernommen — Kanal wie MF-614. | MF-1009 — `apridisk_format::load()` Feld fuer Feld gegen `uft_apridisk.c` gehalten. **UFT konnte keine echte APRIDISK-Datei lesen**, aus drei unabhaengigen Gruenden. **(1) Satztypen:** die obere Haelfte `0xE31D` fehlte GANZ, und SEKTOR und KOMMENTAR waren zusaetzlich vertauscht — UFT hatte `0x00000002` fuer Sektor und `0x00000001` fuer Kommentar, das Orakel `0xE31D0001` bzw. `0xE31D0002`. Kein Typvergleich hat je gegriffen. **(2) Feldbreiten:** `compression` und `header_size` sind **16 Bit** (`get_u16le(&sector_header[4])` bzw. `[6]`); UFT las vier `uint32_t`, womit alles nach dem Typfeld zwei Byte falsch lag. Die Kompressionswerte selbst waren ebenfalls erfunden (`0`/`1` gegen `0x9E90`/`0x3E5A`). **(3) Sektorfelder:** sie stehen IM 16-Byte-Kopf, an `[12]` Kopf, `[13]` Sektor (1-basiert), `[14..15]` u16le Spur. UFT las einen eigenen 8-Byte-Deskriptor aus `pos - 8` mit `cylinder, head, sector, size_code` — also die Bytes, die im echten Format `data_size` sind. `size_code` gibt es nicht; die Sektorgroesse ist mit 512 fest. **Gemessen an einer nach MAMEs load() gebauten Pruefdatei (9153 Byte, 2 Spuren x 1 Kopf x 9 Sektoren): 1 Spur, 0 Sektoren, und `UFT_OK`.** Erfolg ohne Tat. **Ein vierter Fehler kam dabei heraus:** `track->sector_count` blieb 0, weil `uft_track_alloc()` nur Kapazitaet anlegt — die Schranke `rec.sector <= track->sector_count` war nie wahr. Das fiel vorher nicht auf, weil ohnehin kein Satz durchkam. **Auch das Kompressionsformat war erfunden:** UFT hatte einen Byte-Strom mit Zaehl-, Literal- und Laufbloecken (`apridisk_rle_compress/_decompress`). Ein gepackter Satz ist genau drei Byte: u16le Laenge (muss 512 sein) + Fuellbyte. Der zugehoerige Rundlauftest `TEST(apridisk_rle_compression)` war gruen und bewies NICHTS — Packer und Entpacker waren Spiegelbilder voneinander (MF-992-Klasse), und die Datei steht ausserdem in `EXCLUDED_TESTS`, wurde also nie gebaut (MF-1000-Klasse). Ersetzt durch `TEST(apridisk_fuelllauf)`. Rotbeweis: `tests/test_apridisk_gegen_mame.c` — 3 von 6 Faellen rot vor dem Fix, mit zwei Gegenproben (Magic erkannt, Nicht-APRIDISK abgewiesen). **Klasse: MF-961** — dort probte `86f` auf „86BX“, ein Magic, das in keiner echten Datei steht; hier war der ganze Satzaufbau erfunden. **T2 und nicht T1b:** ein gelesener fremder Quelltext belegt die Struktur, nicht die Wirklichkeit — im Korpus liegt kein APRIDISK. | — |
| `atx` | **T2** | `test_atx_interleave_positions`, `test_atx_layout`, `test_atx_roundtrip`, `test_format_probe_fuzz`, `test_plugin_probe_real` | a8rawconv 0.95 (GPL-2-or-later, Referenz-Orakel in src/a8rawconv/, wird nicht gebaut): src/a8rawconv/diskatx.cpp:3-62 (ATXFileHeader/ATXTrackHeader/ATXSectorHeader/ATXTrackChunkHeader mit static_assert auf 48 bzw. 32 Byte), :64-190 read_atx, :216-416 write_atx. Der Schreiber ist hier die staerkere Quelle: er legt Feld fuer Feld fest, was der Leser konsumiert. | MF-467 — Datei-, Spur-, Chunk- und Sektorkopf Feld fuer Feld verglichen. Vier Fehler gefunden und behoben, zwei davon toedlich: (1) Spurkopf-Flags standen bei uns auf 0x14, richtig ist 0x10; (2) der Chunk-Offset stand auf 0x18, richtig ist 0x14 — dadurch kam der Offset aus Fuellbytes, also 0, der Chunk-Scan begann auf dem Spurkopf selbst, fand keine Sektorliste und lieferte eine leere Spur. JEDE ATX-Datei las sich als leere Diskette, ohne Fehlermeldung. (3) Die Dichte wurde von 0x13 gelesen (Fuellbyte) statt von 0x12. (4) Der Leser erwartete eine Tabelle von LE32-Spur-Offsets und eine Spurzahl im Dateikopf bei 0x14 — beides gibt es im Format nicht: 0x14 ist mImageId, und die Spuraufzeichnungen stehen hintereinander und werden der Reihe nach abgelaufen (read_atx:75-101, write_atx:300-317). Neu belegt uebernommen: FDC-Status-Semantik (0x18 zusammen = Adressfeld-CRC, 0x08 allein = Datenfeld-CRC, 0x10 = kein Datenfeld, 0x20 = Deleted-Mark 0xF8, 0x04|0x02 = langer Sektor, 0x40 = weak), doppelte Sektornummern als Phantomsektoren (UFT_SECTOR_DUPLICATE), und die Zuordnung der Weak-Chunks ueber den Index in der SEKTORLISTE statt ueber die Reihenfolge der erzeugten Sektoren. Abgesichert mit tests/test_atx_layout.c gegen ein bytegenau nach diskatx.cpp gebautes Abbild; Rot-Probe: alle drei Faelle fallen auf der alten Fassung um, der erste schon an der Spurzahl. NICHT verifiziert: Verhalten an einem realen ATX-Abbild — keines im Korpus, daher T2 und nicht T1. | — |
| `cfi` | **T2** | `test_cfi_gegen_samdisk`, `test_cfi_schreibt_in_die_datei`, `test_kurzes_cfi_erfindet_keinen_sektor` | SAMdisk (MIT), src/samdisk/cfi.cpp — ReadCFI(); Formatquelle dort im Kopf genannt: Amstrad FDCOPY „compressed floppy image“, web.archive.org/web/20100706002713/http://www.fdos.org/ripcord/rawrite/cfi.html | MF-1004 — `ReadCFI()` Feld fuer Feld gegen `uft_cfi.c` gehalten. SIEBEN Abweichungen gefunden. Sechs in derselben Richtung — das Orakel bricht ab, UFT kuerzte still. D1 leerer Spurblock (samdisk liest ihn als leere Spur und laeuft weiter — UFT verwarf den Rest der DATEI; gemessen an einer Pruefdatei mit leerem Block in der Mitte ging der gesamte zweite Kopf verloren, mit UFT_OK). D3 Ausgabepuffer voll (samdisk: „expanded CFI image is too big“ — UFT klemmte still und erfand die Geometrie danach aus der GEKLEMMTEN Groesse: 2 949 120 / 512 = 5760 Sektoren = 80/2/36). D5 Spurlaenge ueber das Dateiende (samdisk: „short file in CFI track block“). D6 Teilbloecke treffen `track_end` nicht (samdisk: „track data overflows CFI track block“). Blockformat selbst stimmte ueberein: 2-Byte-Kopf, Laenge = lo | ((hi & 0x7F) << 8), Bit 15 = RLE mit einem Fuellbyte. Rotbeweis tests/test_cfi_gegen_samdisk.c, 4 von 6 Faellen rot vor dem Fix. **T2 und nicht T1b**: ein gelesener fremder Quelltext belegt die Struktur, nicht die Wirklichkeit — im Korpus liegt kein CFI. **D7 kam erst durch die SCHREIBSEITE:** `uft_cfi_read_mem()` wies jede Datei unter `CFI_MIN_FILE_SIZE` (512) ab. CFI komprimiert; `uft_cfi_write()` erzeugte aus einer 1x2x9-Diskette (9216 Byte Nutzlast) eine **39 Byte** grosse, strukturell einwandfreie Datei — zwei Spurbloecke, von Hand nachdekodiert, beide genau 4608 Byte entpackend — und der eigene Leser lehnte sie ab. `ReadCFI()` kennt keine solche Untergrenze. **Ein Feldabgleich vergleicht Verhalten; eine Konstante, die das Orakel gar nicht hat, faellt dabei nicht auf** — gefunden hat es der Rundlauf (tests/test_cfi_schreibt_in_die_datei.c). Mutation M1 (Untergrenze zurueckgelegt) faellt beide Zusagen, jede genau die eigene. | — |
| `cqm` | **T2** | `test_cqm_layout`, `test_format_probe_fuzz`, `test_plugin_probe_real` | Primaer: "CopyQM Format (*.cqm) — Disk image layout", RPN, 2023-03-31, https://rio.early8bitz.de/cqm/cqm-format.pdf (abgeleitet aus LibDsk drvqm.c/crctable.c). Gegengelesen: SAMdisk 4.0 (MIT), src/samdisk/cqm.cpp:10-41 CQM_HEADER + :60-79 CRC + :129-146 RLE. | MF-461 — 133-Byte-Kopf Feld fuer Feld verglichen; beide Quellen stimmen ueberall ueberein, und die Primaerquelle sagt ausdruecklich, dass der Block 0x03..0x1B der BPB einer DOS-Diskette ist (was die Feldfolge dort unabhaengig bestaetigt). Der bisherige Leser las ein Layout, das in KEINER der beiden Quellen vorkommt und auch kein BPB ist: Sektorgroesse als 128<<n-Code aus dem Einzelbyte 0x03 statt als LE16-Byteanzahl aus 0x03,0x04; Sektoren/Spur aus 0x08 (das ist die FAT-Kopienzahl) statt aus 0x10,0x11; Koepfe aus 0x09 (Verzeichniseintraege) statt aus 0x12,0x13; Zylinder aus 0x0F (High-Byte von Sektoren/FAT) statt aus 0x5A; Kommentarlaenge aus 0x10,0x11 statt aus 0x6F,0x70; Datenbeginn bei Offset 18 statt bei 133+Kommentarlaenge. Dazu war die RLE-Polaritaet invertiert (positive Zahl = Wiederholung statt Literalfolge). Kopfpruefsumme (Summe ueber alle 133 Byte == 0 mod 256), Daten-CRC (CRC-32 reflektiert mit CopyQMs 6-Bit-Tabellenquirk &0x3f), Sektorbasis 0x71 und Fuellbyte nach Blind-Modus 0x58 fehlten ganz. Alles ersetzt und mit tests/test_cqm_layout.c gegen ein bytegenau nach Spec gebautes Abbild abgesichert; Rot-Probe: alle fuenf Faelle fallen auf der alten Fassung um. | — |
| `d13` | **T2** | `test_d13_layout_verified` | Differenzlauf gegen die Referenz-Implementierung `to_woz2` (Apple-II-Disk-Tools, Commit 639dc1c; registriertes Oracle, docs/ORACLES.md). Die Zuordnung logisch<->physisch ist die IDENTITAET, zweifach belegt: mamedev/mame ap2_dsk.cpp:410 `a2_13sect_format::logical_sector_index(int p) { return p; }` (BSD-3-Clause) und ciderpress2.com/formatdoc/Unadorned-notes.html '13-sector floppies use physical sector order'. Der Kontrast traegt den Beleg: die 16-Sektor-Fassung steht bei :666 und benutzt dos_skewing[]/prodos_skewing[]. | MF-722: linke Seite uft_format_plugin_d13 (Versatz (cyl*13+s)*256, Sektor-ID = s), rechte Seite dieselbe Diskette ueber to_woz2 -> WOZ 2.0 -> uft_apple_gcr_scan_track() auf dem 5-and-3-Weg (MF-721). Gemessen: 454 dekodiert, 454 verglichen, 454 BYTEIDENTISCH. Der 455. ist Spur 0 Sektor 0 — DOS 3.2 schreibt den Bootsektor mit einer ANDEREN 5-and-3-Variante (to_woz2.c deduce_encoding(): ENC_53A statt ENC_53); er wird als alt_encoding BENANNT, nicht geraten, und liefert keine Bytes. Regressionsschutz: tests/test_d13_layout_verified.c — dort auch die Messung, dass d13_open() jede Datei mit falscher Groesse abweist (der 0xE5-Fuellzweig in read_track ist dadurch unerreichbar). | — |
| `d77` | **T2** | `test_d88_header_variants` | pc98.org D88 (D77 teilt das Layout) + MAME d88_dsk — Kopf 688 ODER 672 Byte, Spurtabelle 164 bzw. 160 Eintraege, erster Versatz 0x2B0 oder 0x2A0 | MF-625 | — |
| `dc42` | **T2** | `test_dc42_checksum_roundtrip`, `test_format_probe_fuzz`, `test_plugin_probe_real` | DiscFerret/Mini-vMac DC42 checksum (BE16 word add, ROR32 1) | MF-324 | — |
| `do` | **T2** | `test_apple_do_po_bounds`, `test_do_layout_verified`, `test_do_po_probe_ignores_content`, `test_do_write_roundtrip`, `test_format_probe_fuzz`, `test_plugin_probe_real`, `test_sector_id_on_disk` | Differenzlauf gegen die Referenz-Implementierung `to_woz2` (Apple-II-Disk-Tools, Commit 639dc1c; registriertes Oracle, docs/ORACLES.md) plus die DOS-3.3-Interleave-Tabelle aus a8rawconv diska2.cpp:3-5 als unabhaengige zweite Quelle | MF-716: linke Seite uft_format_plugin_do (Versatz (cyl*16+s)*256, Sektor-ID = logisch s), rechte Seite dieselbe Diskette ueber to_woz2 -> WOZ 2.0 -> uft_apple_gcr_scan_track() in physische Sektoren, verbunden ueber die Interleave-Tabelle. Gemessen: 560 dekodiert, 560 verglichen, 560 BYTEIDENTISCH, 0 fehlend. Der Regressionsschutz steht in tests/test_do_layout_verified.c. Gilt NUR fuer die DOS-Ordnung — `po` war nicht beteiligt und bleibt T3. | — |
| `fdi_pc98` | **T2** | `test_fdi_pc98_gegen_mame` | MAME `src/lib/formats/pc98fdi_dsk.cpp`, **BSD-3-Clause**, Copyright Olivier Galibert, aus `neue-ideen/FORMATS.ZIP` — 110 Zeilen; getragen haben `identify()` (Z. 37-58), `load()` (Z. 60-107) und die Kopffeld-Versaetze (Z. 48-53 / 71-75). Gelesen, nicht uebernommen (Kanal *Spec* nach MF-695). | MF-1026 — `pc98fdi_format` Feld fuer Feld gegen `uft_fdi_pc98.c` gehalten, und **der Abgleich fand KEINEN Fehler**: die Kopffelder (`hsize` 0x08, `psize` 0x0C, `ssize` 0x10, `scnt` 0x14, `sides` 0x18, `ntrk` 0x1C, alle u32le), **beide** Konsistenzbedingungen aus `identify()` (`size == hsize + psize` UND `psize == ssize*scnt*sides*ntrk`), die zylinder-dure Versatzformel (`hsize + ssize*scnt*(track*head_count + head)`) und die 1-basierten Sektornummern (`sects[i].sector = i + 1`) stimmen ueberein. Auch die Geometrie kommt bei beiden aus dem Kopf und nicht aus dem Typfeld bei 0x04 — das liest MAME ueberhaupt nicht.

Das ist die Lage aus MF-1006 (`mgt`), und dort steht die Lehre: **Uebereinstimmung reicht nur, wenn sie bewacht ist statt behauptet.** `tests/test_fdi_pc98_gegen_mame.c` nagelt jede Zahl an einer selbstbeschreibenden 2HD-Datei fest (77 x 2 x 8 x 1024): 154 von 154 Spuren, jeder Sektor mit seinen EIGENEN Bytes. Mutationsmatrix **5 von 5**.

**Und die Matrix hat einen Fehler in meiner eigenen Gegenprobe gefunden.** Die Pruefung „psize passt nicht zur Geometrie“ war zuerst gruen **aus dem falschen Grund**: ich hatte `psize` um einen Sektor erhoeht, damit faellt aber schon die ZWEITE Bedingung (`size == hsize + psize`), und die erste kam nie zum Tragen — die Mutation „erste Bedingung entfernen“ rutschte durch. Isoliert wird sie, indem die GEOMETRIE widerspricht (`scnt` 8 → 9) und die Dateigroesse stimmt. Dieselbe Falle wie in MF-1014, wo 200 Dateien die Laengenpruefung trafen statt der 128er-Grenze. Ein Gegenbeweis, dessen Ablehnungsgrund nicht gemessen ist, beweist nichts.

**T2 und nicht T1b**, weil die Pruefdatei hauseigen ist: es liegt keine fremd erzeugte PC-98-FDI im Korpus, und keines der hier verfuegbaren Werkzeuge schreibt eine (hxcfes Modulliste kennt das Format nicht). | — |
| `jv1` | **T2** | `test_format_probe_fuzz`, `test_jv1_ist_einseitig`, `test_plugin_probe_real` | Tim Mann, „Common File Formats for Emulated TRS-80 Floppy Disks“ (https://www.tim-mann.org/trs80/dskspec.html), abgerufen 2026-09-10 — die Formatbeschreibung selbst, und genau die Quelle, die der Dateikopf schon vorher nannte; unabhaengig bestaetigt durch MAME `formats/trs80_dsk.cpp` (BSD-3-Clause, Dirk Best), in `neue-ideen/formats.zip`: drei Geometrien 35/40/80, alle `head_count = 1`, `sector_base_id = 0` | MF-1016 — zwei Befunde, beide gemessen. **(1) Bei mehr als 40 Spuren nahm der Leser eine ZWEITE SEITE an.** `jv1_detect_geometry()` rechnete `if (total_tracks <= 80 && total_tracks % 2 == 0) { *cyl = total_tracks / 2; *heads = 2; }`. Gemessen an einer Datei von **204 800 Byte** (80 × 10 × 256), deren jede Spur mit ihrer eigenen Nummer gefuellt war: gemeldet wurden **40 Zylinder / 2 Koepfe**, und Zylinder 40 war nicht lesbar — die Spuren 40..79 lagen auf **Kopf 1** statt auf den Zylindern 40..79. JV1 hat keine zweite Seite; Tim Manns Formatbeschreibung sagt woertlich „There are 10 sectors per track (i.e., single density), numbered **0 through 9**, and **only one side**“, und MAMEs Tafel fuehrt 35, 40 und 80 Spuren mit durchweg `head_count = 1`. **(2) Die Sektornummern waren 1..10 statt 0..9.** Der Rumpf rief `uft_format_add_sector(track, s, ...)` mit `s = 0..9`; diese Funktion nimmt **laut ihrem eigenen Kopf** einen 0-basierten Laufindex und **addiert 1** — der Kommentar dort nennt Apple, Amiga und Commodore ausdruecklich als Faelle, fuer die das falsch ist. TRS-80 gehoert dazu. MAMEs Tafel setzt `sector_base_id = 0`. Jetzt `uft_format_add_sector_with_id()`. **Und dabei fiel auf, dass ein gruener Test den Fehler BEWACHT hat:** `tests/test_plugin_probe_real.c` verlangte woertlich „41..79 tracks are only valid when even (two heads)“ und wies 41 Spuren ab. Diese Zusage folgte aus der zweiten Seite, die es nicht gibt — dieselbe Gestalt wie MF-992, wo zehn gruene Tests durch dieselbe falsche Struktur zurueckgelesen haben. Sie ist berichtigt, und genau **eine** Zeile fiel dabei (Zeile 388). **Die Spurgrenze ist begruendet gewaehlt:** 80, weil das die groesste Geometrie ist, die MAMEs Tafel WIRKLICH umsetzt (SSQD 80/1). Sein Beschreibungstext sagt daneben „There's no limit on the number of tracks“ — zwei Aussagen aus derselben Quelle; genommen wird die umgesetzte, nicht die weitere. **Eine Divergenz zwischen den Referenzen wird benannt statt entschieden:** fuer Spur 17 nennt Tim Mann die Adressmarke `0xFA`, MAME setzt `FM_DDAM` (`0xF8`). Beide sind sich einig, dass Spur 17 eine nichtstandardmaessige Marke traegt; welche, sagen sie verschieden. In einer JV1-Datei steht ueberhaupt keine Adressmarke — sie ist ein reiner Sektorabzug —, also entscheidet das hier nichts; als P3-324 gefuehrt fuer den Fall, dass JV1 je in einen Bit- oder Flussstrom geschrieben wird. **Nebenbei behoben:** `jv1_write_track()` hatte **keine** obere Schranke fuer `cyl`/`head`, waehrend `head` in die Versatzrechnung einging — der vierte Fall von „Leseseite geholt, Schreibseite uebersehen“ nach MF-519/529/931. **Mutationsmatrix 4 von 4**: >40 Spuren wieder halbieren (faellt 5) · `add_sector` ohne `_with_id` (**genau 1**) · Kopfschranke beim Lesen entfernt (**genau 1**) · Spurgrenze zurueck auf 40 (faellt 4). **T2 und nicht T1b:** kein JV1 von fremder Hand im Korpus, kein Orakel-Binary verfuegbar — der Abgleich ist Quelltext und Formatbeschreibung gegen den eigenen Leser, mit einer Pruefdatei, deren Inhalt jede Spur und jeden Sektor selbst benennt. | — |
| `korg_dss1` | **T2** | `test_korg_dss1_plugin` | chickensys Korg DSS-1 geometry (80x2x5x1024) | MF-347 | — |
| `lisa_twiggy` | **T2** | `test_lisa_twiggy_plugin` | bitsavers Lisa Twiggy ZCAV zone table (46 tracks/side, 22..15 spt) | MF-349 | — |
| `mfi` | **T2** | `test_fremde_hand_liest`, `test_mfi_layout` | MAME, src/lib/formats/mfi_dsk.h (struct header / struct entry, RESOLUTION_SHIFT = 30, CYLINDER_MASK = 0x3fffffff) und src/lib/formats/mfi_dsk.cpp:81-82 (sign[16] = "MAMEFLOPPYIMAGE", sign_old[16] = "MESSFLOPPYIMAGE", je einschliesslich der abschliessenden Null), identify() vergleicht alle 16 Byte, load() liest die Eintragstabelle ab sizeof(header) = 0x20. Beide Dateien BSD-3-Clause per SPDX-Header; kein Code uebernommen, nur das Layout verglichen. | MF-614 — gefunden im vierten Scout-Zyklus. Der Leser prueste ACHT Byte ("MAMEFLOP"), ein Praefix der echten Kennung: er wies echte MAME-Dateien deshalb nicht ab, sondern nahm sie AN und las sie falsch. Die Spurtabelle las er ab 0x10 (sein eigener Kopfkommentar beschrieb das so) — dort liegen cyl_count und head_count; die Eintraege beginnen bei 0x20. form_factor holte er von 0x08, also aus der Kennung. Die Eintragszahl leitete er aus der Dateigroesse ab statt aus (cyl_count << resolution) * head_count. Alle vier berichtigt; zusaetzlich uebernommen: die Schranken aus identify() (Zylinder <= 84, Aufloesung < 3, Koepfe <= 2). Abgesichert mit tests/test_mfi_layout.c gegen ein bytegenau nach mfi_dsk.h gebautes Abbild; Rotprobe: beide Zusicherungen fallen auf der alten Fassung, die erste schon daran, dass ein verfaelschtes Byte 8 noch als gueltig durchging. NICHT verifiziert: die Spurnutzlast. Das Fixture traegt Kopf und Eintragstabelle, keine echten MFI-Zellendaten — dafuer braucht es eine reale Datei, und im Korpus liegt keine. Deshalb T2 und nicht T1. | — |
| `mgt` | **T2** | `test_durchschreibprobe`, `test_mgt_gegen_mame`, `test_mgt_schreibt_in_die_datei`, `test_mgt_verzeichnis_vollstaendig` | MAME, `src/lib/formats/coupedsk.cpp` — `mgt_format::load()` und `::save()`, **BSD-3-Clause**, Olivier Galibert. Vom Eigentuemer als `neue-ideen/formats.zip` beigesteuert; Herkunftsentscheidung MF-1003. Kein MAME-Code uebernommen — Kanal wie in MF-614 (MAME als benannte Referenz). | MF-1006 — `mgt_format::load()` Feld fuer Feld gegen `uft_mgt.c` gehalten. **Anders als bei `cfi` (MF-1004) fand der Abgleich KEINEN Fehler**: Groesse 819200 = 80*2*10*512, Zylinder 80, Koepfe 2, Sektoren je Spur 10, Sektorgroesse 512, Sektor-IDs 1..10 (MAME `sectors[i].sector_id = i + 1`; UFT `s + MGT_FIRST_SECTOR`) und der Spurversatz — MAME `(track*2 + head) * track_size`, UFT `c * MGT_HEADS + h` bei Spurgroesse 5120 — sind dieselbe seitenverschraenkte Anordnung. **Uebereinstimmung reicht nur, wenn sie bewacht ist statt behauptet**: `tests/test_mgt_gegen_mame.c` nagelt jede dieser Zahlen fest, Mutationsmatrix **3 von 3** (Spurversatz vertauscht / Sektor-IDs 0-basiert / 737280 angenommen — jede faellt genau ihre eigene Zusage). **Zwei benannte Unterschiede, kein Fehler:** (a) 737280 mit 9 Sektoren kennt MAMEs `load()`, aber in seinem `identify()` steht der Vergleich auskommentiert — es erkennt die Variante nie. UFT weist sie ab, und das ist nach MF-729 richtig: 737280 ist die Groesse von PC-720K und Atari-ST-DS, sie als MGT zu beanspruchen waere ein Fehlalarm. (b) 409600 (40 Zylinder) kennt UFT und MAME nicht — UFT ist hier breiter. **Berichtigt P3-312:** MAME rechnet dieselbe Anordnung wie UFT und kennt die IMG-Variante ebenfalls nicht; sie ist eine SAMdisk-Besonderheit. UFT liest MGT richtig, und der als Blocker benannte fehlende `src/samdisk/SAMCoupe.h` ist ersetzt. **T2 und nicht T1b**: ein gelesener fremder Quelltext belegt die Struktur, nicht die Wirklichkeit — im Korpus liegt kein MGT. | — |
| `nfd` | **T2** | `test_nfd_r0` | pc98.org nfdr0/nfdr1 + tomari/d88split nfd2mhlt.pl (r1 skip accounting spec-only, no real r1 corpus yet) | MF-358, MF-360 | — |
| `opus` | **T2** | `test_opd_geometrie`, `test_schreibzusage_erreicht_die_datei` | MF-905 Geometrie aus dem Bootsektor: src/samdisk/opd.h (struct OPD_BOOT) und src/samdisk/opd.cpp — ReadOPD() UND WriteOPD() werten identisch aus (cyls, sectors, heads = flags & 0x10, Groessencode = flags >> 6). SAMdisk ist im Baum vendort unter MIT (src/samdisk/License.txt). Loest die frueher genannte, ausdruecklich UNVERIFIZIERTE Referenz libdsk drvopus.c ab (MF-651). | MF-905 | — |
| `scl` | **T2** | `test_scl_ist_eine_trdos_diskette` | src/samdisk/scl.cpp + src/samdisk/trd.cpp (SizeToCylsTRD, TRD-Konstanten) + src/samdisk/cmd_dir.cpp (TRDOS_DIR) — SAMdisk, MIT, im Baum; unabhaengig bestaetigt durch tools/uft-scout/work/HxCFloppyEmulator/libhxcfe/sources/loaders/scl_loader/scl_loader.c (HxC, GPL-2, **nur gelesen** — Kanal Spec nach MF-695, keine Zeile uebernommen) | MF-1014 — SCL ist kein Abbild, sondern ein ARCHIV, und der Leser muss daraus eine TR-DOS-Diskette ausbauen. Vorzustand gemessen mit einem Wegwerf-Programm gegen den vorhandenen Leser: **1 Zylinder, 1 Kopf, 5 Sektoren gesamt** (Orakel: 80 / 2 / 2560), und Byte 0 der ersten Spur war `A0` — der erste Datenbyte der ersten Datei — statt `"TESTA   "`, des ersten Katalogeintrags. Kopf 1, wo die Daten hingehoeren, wurde mit `-93` abgewiesen. Es gab also **keinen Katalog, keinen Infosatz, und jeder Sektor lag eine ganze Spur zu frueh**. Klasse MF-794 (`sad` las 158 von 160 Spuren an der falschen Stelle). Die vier Summenbytes am Dateiende wurden zudem als fuenfter Sektor mitgezaehlt, den `read_track` nie lieferte. **Neu gefasst gegen zwei unabhaengige Haende:** `uDataLba = 16`, `pb[14] = uDataLba & 0x0f`, `pb[15] = uDataLba >> 4`, die Zylinderregel `SizeToCylsTRD()` und die neun Feldlagen im Infosatz (0x8E1 erster freier Sektor · 0x8E2 erste freie Spur · 0x8E3 Geometriebyte 0x16 · 0x8E4 Dateizahl · 0x8E5..0x8E6 freie Sektoren · 0x8E7 Kennbyte 0x10 · 0x8EA neun Leerzeichen · 0x8F4 geloeschte Dateien · 0x8F5 Diskettenname) stimmen zwischen SAMdisk (MIT) und HxC (GPL-2, nur gelesen) **byteweise**; ein drittes Mal bestaetigt sie der eigene Baum, weil `src/formats/trd/uft_trd.c` die Dateizahl bei `0x8E4` liest. **Und eine Stelle, an der dieses Werkzeug den Orakeln bewusst NICHT folgt:** den Diskettennamen traegt die SCL nicht — SAMdisk setzt dort den Quelldateinamen ein, HxC schreibt `"HxCFE"`, beides erfunden. Hier bleibt das Feld leer („Keine erfundenen Daten“), und der Test nagelt das fest. **Zweiter Befund, dieselbe Datei-Familie:** der Katalogeintrag ist `Name[8] · Typ · Start[9..10] · Laenge[11..12] · Sektoren[13]`; `uft_scl_parse()` fuellte `param[0..2]` mit den Bytes 9,10,11 und las Byte **12** nie — das obere Byte der Laengenangabe. `uft_scl_build()` schrieb dort `h[12] = 0` mit dem Kommentar „TR-DOS in-disk start sector; SCL omits“, zweifach falsch (der Startsektor steht bei h[14]). Benannt in `src/samdisk/cmd_dir.cpp::TRDOS_DIR`. Behoben durch zwei ANGEHAENGTE Felder (`start_address`, `length_bytes`), `param[3]` bleibt fuer die ABI stehen. **Mutationsmatrix 7 von 7**, jede faellt genau ihre eigene Zusage: Geometriebyte 0x16→0 · 128-Grenze entfernt · Kurzdatei-Schranke entfernt · Diskettenname erfunden · Sondenschranke entfernt · h[12] beim Lesen verschluckt (3) · h[12] beim Bauen genullt (1); dazu die Rueckmutation „Daten auf LBA 0“ — der alte Fehler —, die 5 faellt. **Eine eigene Gegenprobe war dabei gruen aus dem falschen Grund:** die erste Fassung nahm 200 Dateien, doch da griff schon die Laengenschranke, nicht die 128er-Grenze — die Mutation fiel nicht. Jetzt wird die Grenze dort geprueft, wo sie liegt: 128 geht durch (der Katalog fuellt genau acht Sektoren), 129 nicht. **T2 und nicht T1b:** im Korpus liegt keine SCL von fremder Hand, und kein Orakel-Binary ist verfuegbar — der Abgleich ist Quelltext gegen Quelltext plus die 32-Bit-Summe, die in der Datei selbst steht (derselbe Weg wie MF-869 und MF-1013). | — |
| `tan` | **T2** | `test_tan_ist_einseitig` | MAME `src/lib/formats/trs80_dsk.cpp`, **BSD-3-Clause**, Dirk Best, aus `neue-ideen/FORMATS.ZIP` — `jv1_format::formats[]` (Z. 98-113), drei Eintraege, je `cell_size 4000, sector_count 10, track_count 35/40/80, head_count 1, sector_base_size 256, sector_base_id 0`. Zweite, unabhaengige Hand: Tim Mann, „Common File Formats for Emulated TRS-80 Floppy Disks“ (https://www.tim-mann.org/trs80/dskspec.html) — „numbered 0 through 9, and only one side“. Dieselben zwei Quellen tragen schon MF-1016. | MF-1026 — `tan` liest dieselbe Anordnung wie `jv1`, und **er trug beide Befunde, die MF-1016 an `jv1` behoben hat**. Das ist die Gestalt von MF-519/MF-529 („Leseseite geholt, Schreibseite uebersehen“), nur zwischen zwei DATEIEN statt zwei Funktionen — und damit der Beleg, dass eine Korrektur an einer Datei nichts ueber ihre Nachbarn sagt.

**(1) Eine zweite Seite wurde erfunden.** `if (tracks <= 40) heads = 1; else { cyl = tracks / 2; heads = 2; }` — gemessen an einer Datei von 204800 Byte (80 x 10 x 256) meldete `open` **40 Zylinder / 2 Koepfe**, und die Spuren 40..79 lagen auf einem Kopf, den es nicht gibt. MAMEs jv1-Tafel fuehrt 35, 40 und 80 Spuren mit durchweg `head_count = 1`; Tim Mann schreibt „only one side“.

**(2) Die Sektornummern waren 1..10 statt 0..9**, weil `uft_format_add_sector()` laut eigenem Kopf einen 0-basierten INDEX nimmt und 1 addiert. MAMEs `sector_base_id` ist **0**. Gemessen: **800 von 800** Sektoren mit falscher ID.

Dazu zwei fehlende obere Schranken in `read_track` und `write_track`: Kopf 1 lieferte 10 Sektoren statt einer Absage, und beim Schreiben haette ein zu grosser Zylinder bestimmt, WOHIN geschrieben wird.

**Ein Punkt wurde gemessen und bewusst NICHT abgewiesen:** die Sonde nimmt auch 179200 Byte an (70 x 10 x 256). Diese Groesse steht in MAMEs Tafel nicht, MAMEs `identify()` wuerde sie verwerfen. Sie bleibt zulaessig, weil ein kopfloser Abzug keine andere Regel hat als seine eigene Arithmetik — gelesen wird sie jetzt aber als **70 einseitige Spuren**, nicht als 35 x 2. UFT ist damit bei der SPURZAHL nachsichtiger als das Orakel und bei der SEITIGKEIT nicht.

Abgenommen an selbstbeschreibenden Pruefdateien (jeder Sektor nennt Spur und Sektornummer) zu 35, 70 und 80 Spuren: 80 von 80 Spuren mit 0-basierten IDs und den EIGENEN Bytes. Mutationsmatrix **3 von 3**; dabei fiel auf, dass die Schranke fuer Zylinder 80 vom `fread` mit-erwischt wird (Versatz genau auf dem Dateiende → `UFT_ERROR_IO`) — sie bleibt, weil ein IO-Fehler eine andere Aussage ist als „diese Spur gibt es nicht“. | — |
| `td0` | **T2** | `test_format_probe_fuzz`, `test_plugin_probe_real`, `test_td0_erfindet_keine_sektoren`, `test_td0_error_marks`, `test_td0_kommentarflag`, `test_td0_signatur` | SAMdisk 4.0 (MIT), src/samdisk/td0.cpp:14-25 TD0_HEADER + :28/:208 Kommentar-Flag | MF-460 — 12-Byte-Kopf Feld fuer Feld verglichen: Signatur, Volume-Sequenz, Check-Signatur, Version, Quelldichte, Laufwerkstyp, Spurdichte, DOS-Modus, Seiten, CRC. Position und Breite stimmen ueberall ueberein. Eine Abweichung gefunden und behoben: Byte 7 war als "Stepping type (0=SS, 1=DS, 2=EDS)" beschrieben, ist aber die Spurdichte mit Bit 7 als Kommentar-Flag — was unser eigener Code in uft_td0_lzss.c:469 bereits so auswertet. BERICHTIGT MF-870: falsch war BEIDES. Die Beschreibung stimmte nicht, und uft_td0_parser_v2.c:412 las das Kommentar-Flag tatsaechlich aus drive_type statt aus stepping - MF-460 hat den Vermerk gesetzt und die Zeile darunter nie nachgezogen. Dazu war die Kompressionserkennung invertiert: die Grossbuchstaben-Kennung galt als komprimiert, obwohl sie die unkomprimierte Fassung bezeichnet. Beides behoben, Rotbeweis tests/test_td0_signatur.c (4 von 6 rot gegen den Vorzustand). | — |
| `udi` | **T2** | `test_udi_gegen_die_spezifikation` | Alex Makeev, „UDI file format Version 1.0 final“, 24.03.2002 — wiedergegeben im Sinclair Wiki „UDI format“ (https://sinclair.wiki.zxnet.co.uk/wiki/UDI_format), abgerufen 2026-09-10: Dateikopf, Spursatz, Typtafel und Pruefsummen-Referenzcode woertlich. **Die Spezifikation des Urhebers schlaegt hier das Orakel** — `src/samdisk/udi.cpp` (MIT, im Baum) rechnet die Pruefsumme mit `int32_t` und weicht dadurch ab | MF-1015 — drei Befunde gegen die Spezifikation des **Urhebers**, und der erste ist der schwerste. **(1) Der Kopf war um vier Byte verschoben.** Das Kopfkommentar des Plugins beschrieb einen Dateikopf **ohne** das 4-Byte-Groessenfeld (`0..3 "UDI!"`, dann angeblich Version bei 4, Zylinder bei 5, Kopf bei 6); wirklich steht bei 4..7 die Dateigroesse minus 4, die Version bei 8, `max_cyl` bei 9, `max_head` bei 10, EXTHDL bei 12..15. `udi_open()` las `data[5]` und `data[6]` — das sind die Bytes 1 und 2 der little-endian **Dateigroesse**. **Gemessen, nicht gerechnet:** eine gueltige UDI von **1 125 620 Byte** (80 Zylinder × 2 Koepfe × 6250 B/Spur) liefert `data[5]=44`, `data[6]=17` und wurde mit **-25 abgewiesen**, weil `max_head > 1` verwarf. Damit war **jede realistisch grosse UDI-Datei unlesbar**; nur Dateien unter 128 KB kamen durch und lieferten eine einzige Spur. Klasse **MF-961** (`86f` probte auf ein Magic, das in keiner echten Datei steht) — und wie dort meldete die Merkmalstafel dabei „Read: SUPPORTED“. **(2) Die Taktmarken folgen JEDEM zerlegbaren Spurtyp.** Der Urheber fuehrt 0x00 MFM, 0x01 FM, 0x02 gemischt, 0x80..0x83 schwach — alle mit CLK-Feld; nur 0xE0 (Microdrive) und 0xF0 (zlib) haben keines. `udi_find_track()` sprang nur bei `ttype == 0x00` darueber, also lag hinter einer FM- oder gemischten Spur jede weitere Spur um `CLEN = TLEN/8 + (TLEN%8+7)/8` Byte daneben — bei 6250 Byte je Spur sind das 782. Klasse **MF-794**. Das Kopfkommentar behauptete „Optional: … for MFM type 0x00“, eine Spec-Aussage ohne Quelle, die der Urheber widerlegt. **(3) Drei Pruefsummen im Baum, keine zwei gleich.** Der Urheber gibt den Code woertlich an, und er invertiert **je Byte** statt einmal am Anfang und Ende. Gemessen an „UDI!“: Spezifikation `196DC161`, `uft_udi.c` `196DC161` (**richtig**), das Plugin `C7D6E182` (gewoehnliches CRC-32, falsch), `src/samdisk/udi.cpp` `138C9487` (falsch — `int32_t`, also arithmetischer Shift). Die falsche Fassung stand in `udi_write_track()` und schrieb den Vier-Byte-Abschluss **jeder von UFT geschriebenen UDI**: eine stille Veraenderung, die die Datei selbst als beschaedigt ausweist. Die richtige lag in `uft_udi.c` — der Datei, die niemand ruft. **Das Orakel wurde hier ueberstimmt, und zwar begruendet:** SAMdisk ist eine Referenz, kein Beweis; gegen den Referenzcode des Urhebers gehalten faellt es. Dieselbe Lage wie MF-961, wo die Spezifikation des Urhebers gegen ein erfundenes Magic entschied. **Zwei weitere Entscheidungen, beide gemessen statt geraten:** `UFT_FORMAT_CAP_FLUX` bleibt **gesetzt**, weil `src/core/uft_disk_convert.c:206` das Bit fuer die **Verlustmeldung** auswertet — UDI traegt mit einer Taktmarke je Byte wirklich Zellenwissen, und das Bit zu entfernen wuerde eine wahre Verlustwarnung unterdruecken. Die Typen 0x80..0x83, 0xE0 und 0xF0 werden **benannt abgewiesen** statt geraten, weil ihr Satzaufbau hier nicht gemessen ist und eine geratene Satzlaenge genau Befund 2 wiederholen wuerde. **Berichtigt wurde auch eine Attribution (MF-636):** hier stand „UDI specification by Simon Owen“ — Simon Owen ist der Autor von SAMdisk, nicht von UDI, und diese falsche Zuschreibung war zugleich der Grund, warum der Kopf darunter falsch beschrieben war. **Mutationsmatrix 6 von 6**: Kopfversaetze zurueck (faellt 4, darunter die Zusage „die echte Datei laesst sich oeffnen“) · Taktmarken nur bei MFM (1) · Pruefsumme zurueck auf CRC-32 (1) · unbekannte Typen durchlassen (3, je Typ eine) · jede Version annehmen (1) · Kopfzahl maskieren wie SAMdisk (1). **T2 und nicht T1b:** kein UDI von fremder Hand im Korpus, und kein Orakel-Binary verfuegbar. Der Test traegt deshalb seine **eigene** Umschrift des Referenzcodes — faellt die Fassung im Plugin auf eine andere zurueck, faellt der Test. | — |
| `v9t9` | **T2** | `test_v9t9_gegen_mame` | MAME `src/lib/formats/ti99_dsk.cpp` / `.h`, **LGPL-2.1+**, Copyright Michael Zapf, aus `neue-ideen/FORMATS.ZIP`. **Gelesen, nicht uebernommen** — Kanal *Spec* nach MF-695, eigenstaendige Umsetzung nach dem dokumentierenden Kommentar und den dort genannten Zahlen; keine Zeile Quelltext uebertragen. Getragen haben vier Stellen: Z. 883-908 (der Kommentar zur Spurreihenfolge), Z. 936-944 (`identify()`, die sieben Groessen), Z. 989-991 (die 768-Byte-Fehlsektorkarte), Z. 1077 (die Versatzformel) und `ti99_dsk.h:70-84` (die VIB-Feldlagen). **Die Lizenz ist ausdruecklich genannt, weil sie eine andere ist als bei den uebrigen MAME-Formatklassen dieser Runde** (victor9k, trs80, pc98fdi sind BSD-3-Clause) — nach MF-636 ist eine Attribution eine rechtliche Aussage. | MF-1027 — `ti99_sdf_format` gegen `uft_v9t9.c` gehalten. **Fuenf Befunde, und der erste bedeutete, dass 78 von 80 Spuren jeder zweiseitigen V9T9-Datei am falschen Versatz gelesen wurden.**

**(1) Die Spurreihenfolge war an ZWEI unabhaengigen Stellen falsch.** MAMEs Kommentar (Z. 883-908) sagt woertlich: „The TI file system orders all tracks on side 0 as going inwards, and then all tracks on side 1 going outwards — 00 01 ... 38 39 (side 0) / 79 78 ... 41 40 (side 1). The SDF format stores the tracks and their sectors in logical order.“ Die Datei ist also **kopf-dur**, und auf Seite 1 laeuft die Spurzahl **rueckwaerts**. UFT rechnete `(cyl * heads + head) * spt * 256` — zylinder-verschraenkt UND ohne Umkehrung. Gemessen mit einem Wegwerf-Programm gegen MAMEs Formel (`logicaltrack = (head==0)? track : (2*trackcount-track-1)`, Z. 1077):

    SSSD  ( 92160 Byte, 40 Spuren)    0 von 40 falsch
    DSSD  (184320 Byte, 80 Spuren)   78 von 80 falsch, bis 179712
    DSDD  (368640 Byte, 80 Spuren)   78 von 80 falsch, bis 359424

**Warum es nie aufgefallen ist:** bei einseitigen Dateien geht die alte Formel in die richtige ueber, weil `heads == 1` aus `cyl*1+0` genau `cyl` macht. Zweiseitig trafen genau zwei Spuren — (0,0) und, **rein zufaellig**, (1,26), weil `2t+1 = 79-t` bei t = 26 aufgeht. Kopf 1 Spur 0 ist die AEUSSERSTE Spur der zweiten Seite und liegt bei Versatz 182016 (logische Spur 79); UFT suchte sie an Position 2304.

**(2) Vier von sieben Dateigroessen wurden abgewiesen.** Angenommen waren 92160, 184320 und 368640; MAMEs `identify()` (Z. 936-944) fuehrt dazu 163840 (SSDD16), 327680 (DSDD16), 737280 (DSDD80) und 1474560 (DSQD). Die beiden 80-Spur-Formate waren ohnehin unerreichbar, weil `p->cyl` fest auf 40 stand.

**(3) Die VIB wurde nie gelesen.** Sektor 0 einer formatierten TI-Diskette ist der Volume Information Block und traegt `"DSK"` bei Versatz 13, Sektoren je Spur bei 12, Spuren je Seite bei 17, Seitenzahl bei 18 (MAME `ti99_dsk.h:70-84`; die Struktur ist genau 256 Byte und belegt den Sektor vollstaendig). Die Sonde ignorierte den Inhalt vollstaendig (`(void)d; (void)s;`).

**(4) 184320 Byte sind ZWEIDEUTIG, und die Zweideutigkeit war still aufgeloest.** SSDD (1 Seite, 40 Spuren, 18 Sektoren) und DSSD (2 Seiten, 40 Spuren, 9 Sektoren) haben dieselbe Groesse. MAMEs Kommentar sagt genau das („SSDD happens to be the same size as DSSD in the sector dump format, so we need to ask the VIB“) und fragt deshalb die VIB; ohne VIB nimmt es DSSD an. UFT nahm **immer** DSSD — also MAMEs Rueckfall, auch wenn die Datei selbst SSDD sagt.

**(5) Keine obere Schranke** fuer Zylinder oder Kopf. Das wiegt hier mehr als sonst: die Umkehrung `2*cyl_max - cyl - 1` kann aus einem zu grossen Zylinder einen **negativen** Versatz machen.

**Eine Stelle, an der dieses Werkzeug dem Orakel bewusst NICHT folgt:** MAME benutzt die VIB-Angaben auch dann, wenn sie der Dateigroesse widersprechen, und gibt nur eine Warnung aus. Hier ist die VIB eine **Behauptung** und die Dateigroesse eine **Tatsache** — bei Widerspruch gewinnt die Groesse, weil eine Geometrie, die nicht in die Datei passt, beim Lesen hinter das Dateiende fuehrt und damit genau die erfundenen Daten erzeugt, die dieses Werkzeug nicht liefern soll. Der Test nagelt auch diese Abweichung fest.

Abgenommen an **selbstbeschreibenden** Pruefdateien (jeder Sektor nennt Kopf, Spur und Sektornummer), gebaut nach MAMEs Anordnung und nicht nach UFTs Code: alle sieben Groessen mit richtiger Geometrie, die Variante mit Fehlsektorkarte (+768 Byte), alle 80 Spuren einer DSSD mit 0-basierten IDs und den EIGENEN Bytes, die drei Faelle der 184320-Zweideutigkeit, die widersprechende VIB, die Grenzen und die MF-729-Eichung (Nullpuffer unter 50). Mutationsmatrix **9 von 9**.

**T2 und nicht T1b:** im Korpus liegt kein V9T9 von fremder Hand. Der Versuch aus MF-1021 ist benannt gescheitert — hxcfe erzeugte 184320 Byte, meldete Erfolg, und die Datei war **100 % 0xF6**, weil sein RAW-Lader die TI-Geometrie nicht kennt (P3-327). Die Datei liegt als Beleg im Korpus und traegt keine Stufe. | — |
| `vdk` | **T2** | `test_vdk_gegen_mame` | MAME `formats/vdk_dsk.cpp` + `vdk_dsk.h` (BSD-3-Clause, Dirk Best), in `neue-ideen/formats.zip` — Kennung `'d','k'`, `header_size = header[3] * 0x100 + header[2]`, `track_count = header[8]`, `head_count = header[9]`, `SECTOR_COUNT = 18`, `SECTOR_SIZE = 256`, `FIRST_SECTOR_ID = 1`, Spur aussen / Kopf innen; dazu `save()`, das den 12-Byte-Kopf selbst schreibt | MF-1018 — drei Befunde, alle gemessen an Pruefdateien, deren Spuren und Sektoren sich selbst benennen (Fuellbyte = 0x40 + Spur, Byte 1 = Sektornummer, Byte 2 = Kopf). **(1) Die Kopfgroesse lag in einem `uint8_t`.** `typedef struct { FILE *file; uint8_t hdr_size, tracks, sides; }` — gefuellt aus `uft_read_le16(hdr + 2)`, also ein 16-Bit-Wert in einem 8-Bit-Feld. Bei einem Kopf von **exakt 0x0100 Byte** wird daraus 0, und der Leser beginnt bei Versatz 0. Gemessen lieferte Sektor 1 dann `Byte 0 = 64, Byte 1 = 6B` — das ist `'d' 'k'`, **die eigene Kennung der Datei als Sektordaten**; insgesamt 244 Byte Kopf-Fuellwerk standen in den Sektoren. MAME liest `uint8_t header[0x100]`, **bevor** es die Kopfgroesse benutzt — ein Kopf bis 256 Byte ist im Format also vorgesehen (VDK traegt dort unter anderem den Diskettennamen). Klasse MF-796/MF-1017. **(2) Eine Geometrie, die die Datei verneint.** `if (p->tracks == 0) p->tracks = 35;` — steht im Kopf eine 0, sagt die Datei „keine Spuren“; gemessen wurden daraus **35 Zylinder**. Die Sektoren selbst waren dank MF-980 ehrlich als fehlend gekennzeichnet, aber die ZAHL war erfunden. MAME nimmt `header[8]` und `header[9]` woertlich. **(3) Das Kompressionsbyte wurde ignoriert.** `hdr[11]` sagt, ob die Sektordaten gepackt sind; gesetzt las UFT sie trotzdem als Sektorabzug und meldete Erfolg. **Hier ist das Orakel selbst stumm** — MAMEs `load()` prueft das Byte nicht. Abgesagt wird es hier trotzdem, weil „Keine erfundenen Daten“ schwerer wiegt als Gleichlauf mit dem Orakel. **Und was gemessen wurde und STIMMT:** die Sektor-IDs. `uft_format_add_sector(track, s, ...)` mit `s = 0..17` ergibt nach der `+1` dieser Funktion **1..18**, und MAME setzt `FIRST_SECTOR_ID = 1`. Anders als bei `jv1` (MF-1016) ist das hier richtig — es wird deshalb **geprueft und nicht angefasst**. **Nebenbei behoben:** die obere Schranke fuer `cyl`/`head` fehlte auf **beiden** Seiten. Beim Schreiben wiegt das schwerer, weil ein `fwrite` jenseits des Dateiendes die Datei VERLAENGERT — der fuenfte Fall dieser Art nach MF-519/529/931/1016. **Eine eigene Zusicherung war zu schwach und wurde verstaerkt:** die Probe auf das Kopf-Fuellwerk 0x99 sah nur `data[0]` an und blieb unter der Mutation gruen, obwohl das Fuellwerk ab Byte 12 des ersten Sektors sehr wohl drinstand. Jetzt wird jedes Byte abgetastet, und die Mutation meldet 244 Treffer. Eine Zusicherung, die nur eine Stelle abtastet, ist eine, die kaum feuern kann. **Die Sonde bleibt unangetastet:** `tests/test_disk_open_fuzz.c` verlangt ausdruecklich, dass sie eine Datei mit der Kennung „dk“ annimmt. Die Pruefung der Kopffelder gehoert ins Oeffnen — die Sonde sagt, WER es versuchen soll, das Oeffnen urteilt ueber die Datei. **Mutationsmatrix 4 von 4**: Kopfgroesse zurueck auf `uint8_t` (faellt 2, darunter die 244 Fuellbytes) · Spuren/Seiten wieder auf 35/1 heben (2) · Kompressionsbyte ignorieren (**genau 1**) · obere Schranken entfernen (2). **T2 und nicht T1b:** kein VDK von fremder Hand im Korpus, kein Orakel-Binary verfuegbar. | — |
| `victor9k` | **T2** | `test_victor9k_gegen_mame` | MAME `src/lib/formats/victor9k_dsk.cpp` / `.h`, **BSD-3-Clause**, Copyright Curt Coder, aus `neue-ideen/FORMATS.ZIP`. Getragen haben vier Aussagen: `sectors_per_track[2][80]` (Z. 392-414), `formats[]` (Z. 378-386, SSDD 1224 / DSDD 2391), `find_size()` (Z. 136-149, genau `sector_count * sector_base_size`) und `build_sector_description()` (Z. 280-289, `sectors[i].sector_id = i`). Gelesen und ausgefuehrt, nicht uebernommen (Kanal *Spec* nach MF-695; die Lizenz erlaubt mehr, gebraucht wurde es nicht). | MF-1026 — `victor9k_format` Feld fuer Feld gegen `uft_victor9k.c` gehalten. **Fuenf Befunde, und der erste allein bedeutete, dass UFT KEINE echte zweiseitige Victor-Datei oeffnen konnte.** Victor 9000 hat **neun** Geschwindigkeitszonen und **zwei verschiedene** Zonentafeln, eine je Kopf; der Kopf des Plugins beschrieb acht Zonen mit EINER Tafel fuer beide.

**(1)** `VIC9K_DS_SIZE` war 1253376 = 1224 x 2 x 512. Wirklich sind es **1224192** = 2391 x 512, denn Kopf 1 hat 1167 statt 1224 Sektoren — 57 Sektoren Differenz. `vic9k_open()` wies jede andere Groesse mit `UFT_ERROR_FORMAT_INVALID` ab, gemessen `rc=-25`. Klasse MF-1015 (`udi` wies jede realistisch grosse Datei ab).

**(2)** Kopf 1 wurde mit der Tafel von Kopf 0 gelesen: **57 von 80** Spuren mit falscher Sektorzahl, **79 von 80** am falschen Versatz, bei Spur 79 um **28672 Byte** (56 Sektoren).

**(3)** Auf Kopf 0 lagen zwei Zonengrenzen um eins daneben (Spur 48: 15 statt 14; Spur 70: 12 statt 13). **Weil sich +1 und -1 aufheben, blieb die Summe 1224** — die Groessenpruefung konnte es NIE bemerken, und **22 Spuren (49..70) wurden 512 Byte zu hoch gelesen**. Spur 48 gab einen 15. Sektor aus, der der Spur 49 gehoert; Spur 70 lieferte ihren 13. Sektor nie. Das ist die teuerste Gestalt dieses Baums: eine Pruefung, die stimmt, weil sich zwei Fehler gegenseitig decken.

**(4)** Die Sektornummern waren 1..n statt 0..n-1; MAME setzt `sectors[i].sector_id = i`. Gestalt von MF-1016 (`jv1`).

**(5)** Ein Zylinder ausserhalb 0..79 kam als `UFT_OK` mit null Sektoren zurueck — `vic9k_spt()` gab 0, die Schleife lief nicht, und der Aufrufer bekam Erfolg fuer eine Spur, die es nicht gibt („Erfolg ohne Tat“).

Bestaetigt durch MAMEs eigene Konsistenz: `formats[]` fuehrt DSDD mit **2391** Sektoren, und 1224 + 1167 = 2391. Abgenommen an einer nach MAMEs Arithmetik gebauten **selbstbeschreibenden** Datei (jeder Sektor nennt Kopf, Spur und Sektornummer): 160 von 160 Spuren mit richtiger Sektorzahl, richtiger 0-basierter ID und den EIGENEN Bytes. Mutationsmatrix **5 von 5**. **T2 und nicht T1b**, weil die Pruefdatei hauseigen ist — hxcfe kennt Victor 9000 nicht, FluxEngine ist hier nicht baubar.

**Am Rand gefunden, nicht angefasst:** `src/formats/victor/victor9k.c` (verwaist, `docs/orphan_baseline.txt:212`) fuehrt eine **dritte** Geometrie — flach 15 Sektoren fuer Spur 38..79, Summe 1285 Sektoren = 657920 Byte, eine Groesse, die keine Victor-Diskette hat. Es gibt keinen Aufrufer; ein Hinweis steht jetzt in der Datei, damit die naechste Hand die falschen Zahlen nicht erbt (MF-699: erst der Ersatz, dann die Loeschung). | — |
| `woz` | **T2** | `test_apple_gcr_6and2`, `test_diskcopy`, `test_format_probe_fuzz`, `test_moof_roundtrip`, `test_nib_ring_und_blindzone`, `test_plugin_probe_real`, `test_woz_roundtrip`, `test_woz_writer` | Applesauce WOZ reference v1/v2/2.1 (chunk layout, CRC32, WRIT logical refs) | MF-317, MF-357, MF-361 | — |
| `2img` | **T3** | `test_2img_nib_stride`, `test_format_probe_fuzz`, `test_plugin_probe_real` | — | — | — |
| `cas` | **T3** | — | — | — | — |
| `cpm` | **T3** | `test_cpm_fs` | — | — | — |
| `dcm` | **T3** | — | — | — | — |
| `dim` | **T3** | `test_dim_kennung_und_groesse` | — | — | — |
| `dms` | **T3** | `test_dms_plugin_gegen_bibliothek`, `test_uft_dms` | — | — | — |
| `edk` | **T3** | — | — | — | — |
| `fds` | **T3** | — | — | — | — |
| `hardsector` | **T3** | `test_hardsector_geometry` | — | — | — |
| `ipf` | **T3** | `test_format_probe_fuzz`, `test_ipf_air_accessors`, `test_ipf_helper`, `test_ipf_sonde_beansprucht_nur_ipf`, `test_plugin_probe_real` | — | — | — |
| `logical` | **T3** | — | — | — | — |
| `myz80` | **T3** | — | — | — | — |
| `nanowasp` | **T3** | — | — | — | — |
| `nib` | **T3** | `test_format_probe_fuzz`, `test_nib_ring_und_blindzone`, `test_plugin_probe_real` | — | — | — |
| `posix` | **T3** | — | — | — | — |
| `pri` | **T3** | — | — | — | — |
| `pro` | **T3** | `test_atari`, `test_atari_dir_past_end`, `test_atari_verlorene_sektoren`, `test_floppy_formats`, `test_pro_schreibt_nicht`, `test_st_plugin` | — | — | — |
| `qrst` | **T3** | — | — | — | — |
| `rcpmfs` | **T3** | — | — | — | — |
| `syn` | **T3** | — | — | — | — |
| `xdm86` | **T3** | — | — | — | — |
