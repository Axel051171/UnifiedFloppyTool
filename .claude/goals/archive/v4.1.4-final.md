# GOAL: v4.1.4-rc1 → v4.1.4 final — Doc-Reparatur + Tag-Pfad

**Bezug:**
- `releases/v4.1.4/P2.4_CHECKLIST.md` (Original-Plan, teilweise obsolet)
- `docs/REFACTOR_TASKS.md` P2.4
- Verifikations-Skript: `uft_p24_verify.sh`

**Status-Legende:** ✅ done · 🟡 in progress · ⬜ pending · 🛑 STOP-Gate

**Branch:** `main` (NICHT `refactor/type-driven-hal` — siehe BEFUND 2)
**Source-of-truth-Konflikt:** Bei Widerspruch zwischen `REFACTOR_TASKS.md`-Tabelle
und `RELEASE_NOTES.md` gewinnt **der Code**, nicht die Doku.

---

## Vier Befunde, die dieses Goal adressiert

| # | Befund | Auswirkung | Phase |
|---|--------|------------|-------|
| 1 | `RELEASE_NOTES.md` listet P1.20/21/22/23 als "queued for v4.1.5", obwohl MF-200/201/202/205/206 alle im RC1-Tag (`dd0ef4f`) als Vorfahren enthalten sind | RC1 lügt über den eigenen Scope | 1 |
| 2 | `P2.4_CHECKLIST.md` Step 4 verlangt `git merge --squash refactor/type-driven-hal` — main ist aber bereits 5 Commits **voraus** vom Refactor-Branch | Workflow nicht ausführbar wie geschrieben | 1 |
| 3 | PR #26 mit Titel "(DO NOT MERGE)" wurde in main gemerged (`21ff31b5`); Inhalt test-only, aber Process-Smell | Reviewer-Verwirrung in 6 Monaten | 1 |
| 4 | HIL Hardware-Tier ist `NOT_RUN`, `audit/rc1_field_notes.md` zeigt 14 NOT_RUN / 1 PASS | Echter Tag-Blocker | 3 |

**Verifikationsbeweis pro Befund:**
```bash
# Befund 1 — RELEASE_NOTES vs Code
grep -c "uft_gw_" src/fluxcapturejob.cpp src/fluxwritejob.cpp
# → 6 Treffer, alle in Removal-Kommentaren (MF-200/201)
git grep -nE "raw_handle\s*\(|gwDevice\s*\(" -- src/ include/
# → nur Kommentar-Leichen, 0 aktive Aufrufe (MF-202)
grep -n "ProviderV2Variant" src/hardwaretab.cpp
# → existiert (MF-205/206)

# Befund 2
git rev-list --left-right --count main...origin/refactor/type-driven-hal
# → "5    0"  (main +5, refactor +0)

# Befund 3
git log --oneline --grep="DO NOT MERGE" main
# → 21ff31b5

# Befund 4
grep -c "NOT_RUN" audit/rc1_field_notes.md releases/v4.1.4-rc1/hil_report.md
```

---

## PHASE 1 — Doc-Reparatur (heute, am Schreibtisch, 3 Commits)

Jeder Commit ist atomar. Build + Tests müssen am Ende jedes Commits grün sein
(Doc-Only-Änderungen, also strukturell stabil).

### Task MF-240 — RELEASE_NOTES.md ehrlich machen

**Datei:** `RELEASE_NOTES.md`
**Section:** `## Known limitations / queued for v4.1.5`

**Was raus muss (3 Bullets):**
1. `FluxCaptureJob`/`FluxWriteJob` claim — IST DONE (MF-200/201)
2. `raw_handle()` removal claim — IST DONE (MF-202)
3. non-GW V2 routing claim — IST DONE (MF-205/206)

**Was rein muss (ehrlicher v4.1.5+ Backlog):**
- **M3.x Production-Transporte** — die 8 non-GW V2-Provider sind honest-stub,
  produktive Transporte (M3.1 SCP-Direct libusb + SDK-vendoring, M3.2 XUM1541
  libusb, M3.3 Applesauce QSerialPort, M3.4 UFI-Backend Windows/macOS).
  Tracker: `docs/M3_HAL_PLAN.md`.
- **ARCH-7 sub-B + sub-C** (VID/PID) — SCP-Header vs GUI-Hint Widerspruch
  (`0x16C0:0x0753` vs `0x16D0:0x0F8C`); ADF-Copy ↔ Applesauce teilen
  `0x16C0:0x0483` (Stock-Teensy-ID, braucht String-Descriptor-Disambig).
  Tracker: `audit/ARCH-7_VID_PID.md`, task #121.
- **ARCH-8** — offizielle Protokoll-Quellen vendorn (GW `cmd.h`, SCP SDK,
  OpenCBM, fluxengine/DTC) → D1 von `recalled` auf `vendored` heben →
  `audit.yml` wird echtes CI-Gate.
- **§1.2 Loss-Report-Wiring** in alle 44 `convert_*`-Pfade (~10 Zeilen Glue
  pro Konvertierer).
- **§6.1 Emulator-CI** (WinUAE/VICE) — geplant 4.3 (`docs/M3_HAL_PLAN.md` §M3.5).
- **Plugin-Compliance-Populierung** — `spec_status` 15/80, `features` 5/80,
  `compat_entries` 1/80, `is_stub` 0/287 → iterativ in 4.2.x.
- **ARCH-9** — XUM1541 `.dylib`-Load-Path für macOS.

**Verifikation:**
```bash
bash uft_p24_verify.sh 2>&1 | grep -E "lügt|✓.*RELEASE_NOTES"
# Erwartet: keine "lügt"-Zeilen mehr
```

**Commit-Message:**
```
docs(release): align RELEASE_NOTES known-limitations with reality (MF-240)

RC1 (dd0ef4f) shipped with RELEASE_NOTES listing P1.20/P1.21/P1.22/P1.23
as "queued for v4.1.5". Code-Audit (uft_p24_verify.sh §2) zeigt: alle
vier sind in der gleichen Commit-Range längst gelandet:

  P1.20 FluxCaptureJob → V2 outcome surface     MF-200 (652839de)
  P1.21 FluxWriteJob → V2 outcome surface       MF-201 (bf7dd467)
  P1.22 raw_handle()/gwDevice() removal         MF-202 (2d20aec9)
  P1.23 8 non-GW providers via ProviderV2Variant MF-205 (820a0b3f)
                                                MF-206 (0dcaa92b)

MF-235 hatte CHANGELOG-Self-Description gefixt aber RELEASE_NOTES
übersehen. Dieser Commit holt das nach.

Der Known-Limitations-Block wird durch den echten v4.1.5+ Backlog
ersetzt: M3.x Transporte, ARCH-7 sub-B/C, ARCH-8 Quell-Vendoring,
§1.2 Loss-Report-Wiring, §6.1 Emulator-CI, Plugin-Compliance-
Populierung, ARCH-9 macOS XUM1541.

Doc-only. Kein src/ touched.
```

---

### Task MF-241 — P2.4_CHECKLIST.md Step 4 reparieren

**Datei:** `releases/v4.1.4/P2.4_CHECKLIST.md`
**Section:** `## Step 4 — ⚠ Squash-merge to main`

**Begründung:** Beweis `git rev-list --left-right --count main...origin/refactor/type-driven-hal`
liefert `5    0` — main ist 5 Commits voraus (PR #26 + 4 hardening-Commits
MF-236..MF-239), refactor-Branch hat nichts was main nicht hätte. Ein
`git merge --squash refactor/type-driven-hal` wäre ein No-Op + verlöre die
einzeln-vorhandenen Squash-Refactor-Commits.

**Neuer Wortlaut Step 4 (Vorschlag):**
```markdown
## Step 4 — ⚠ Branch-Status verifizieren (kein Merge mehr nötig)

`main` HEAD enthält bereits den vollen Type-Driven-HAL-Refactor (MF-150…
MF-235) sowie die v4.1.5-Hardening-Test-Additions (MF-236…MF-239). Der
ursprüngliche Plan "Squash-Merge refactor/type-driven-hal → main" ist
obsolet — die Integration hat sich anders ergeben.

- [ ] `git rev-list --left-right --count main...origin/refactor/type-driven-hal`
      liefert `N    0` mit N≥5 — refactor-Branch hat 0 Commits voraus.
- [ ] `git diff main origin/refactor/type-driven-hal -- src/ include/` ist
      leer ODER nur erwartete Hardening-Test-Differenzen.
- [ ] Der Tag `v4.1.4` wird direkt auf `main` HEAD gesetzt — kein zusätzlicher
      Merge-Commit.
- [ ] PR #26 ("DO NOT MERGE"-Title) wurde 21ff31b5 gemerged — siehe MF-242
      git-notes Erklärung.
```

**Commit-Message:**
```
docs(release): rewrite P2.4 Step 4 — squash-merge is obsolete (MF-241)

main ist bereits 5 Commits voraus vom refactor/type-driven-hal-Branch.
git merge --squash wäre ein No-Op. Step 4 dokumentiert jetzt den
tatsächlichen Integration-State statt eines nicht-ausführbaren Plans.
```

---

### Task MF-242 — PR #26 "DO NOT MERGE"-Smell dokumentieren

**Was:** `git notes` auf den Merge-Commit `21ff31b5` setzen, der erklärt
warum der Merge trotz Titel passiert ist (z.B. "Title-Tippfehler, sollte
'CI-VERIFY before MERGE' heißen — Inhalt rein test-only, kein src/
berührt, post-hoc verifiziert").

**Commands:**
```bash
git notes add -m "Title was a misnomer — 'DO NOT MERGE' meant 'do not merge until CI green'. Content is test-only (4 files under tests/unit/, 0 src/ files touched). Verified post-hoc: see audit/test_coverage/COVERAGE_AUDIT.md Lücke #1 + #2. No revert needed." 21ff31b5
git push origin refs/notes/commits
```

**Verifikation:**
```bash
git notes show 21ff31b5
# → soll die Erklärung zeigen
```

**Commit-Message:** entfällt — `git notes` ist kein normaler Commit.
Stattdessen in `releases/v4.1.4/RELEASE_NARRATIVE.md` (neu, optional)
festhalten falls future-proof Doku gewünscht.

---

## PHASE 2 — Entscheidung GPG-Signing (1 Frage an Axel)

**Frage:** Soll der finale `v4.1.4`-Tag GPG-signed sein?

- **JA** → `gpg --list-secret-keys` muss eine UID liefern; falls nicht,
  vor Phase 4 einen Signing-Subkey einrichten und `git config user.signingkey`
  setzen. Tag-Command in Phase 4: `git tag -s v4.1.4 -m "..."`.
- **NEIN** → Tag-Command: `git tag -a v4.1.4 -m "...

UNSIGNED — explicit decision 2026-05-XX; v4.1.4-rc1 was also unsigned."`

**STOP — Phase 2 erfordert explizite Antwort von Axel. Nicht selbst entscheiden.**

---

## PHASE 3 — 🛑 HIL-Gate (Hardware-only, nicht automatisierbar)

**Pre-condition:** Phase 1 grün, Phase 2 entschieden.

**Was Axel an seinem Rig tun muss:**

1. **Baseline-SHAs aus main @ MF-149 (pre-refactor) ziehen** falls noch
   nicht vorhanden — `tests/HARDWARE_TRUTH_TESTS.md` §"Pre-refactor
   baseline" enthält die Commands. Falls bereits in
   `tests/baseline_artifacts/` vorhanden: überspringen.

2. **Greaseweazle gegen Baseline** — die 10 Checks aus
   `tests/HARDWARE_TRUTH_TESTS.md` §Greaseweazle:
   - `read_raw_flux` → SHA-256-Match gegen Baseline (byte-identisch)
   - `write_raw_flux` → blank → re-read → byte-identisch
   - `set_motor(true)` audible spin-up
   - `seek(0/40/80)` — kein Head-Knock
   - `recalibrate()` — clean return to track 0
   - `measure_rpm()` × 50 Revs — ±0.5 RPM vs Baseline
   - `detect_drive()` korrekt
   - `detect_drive()` mit no-drive → `DriveAbsent` (NICHT `ProviderError`)
   - Bootloader-Mode → actionable `ProviderError` mit what/why/fix
   - GUI: jeder Hardware-Tab-Button feuert exakt ein Signal

3. **Field-Notes aktualisieren:** `audit/rc1_field_notes.md` — die NOT_RUN-
   Zeilen mit PASS/FAIL + Datum + Device befüllen.

4. **HIL-Report regenerieren:** `python tests/hil/run_hil.py --version v4.1.4-rc1`
   → `releases/v4.1.4/hil_report.md` (mirror der RC1-Datei) mit den
   `active` Catalog-Einträgen.

**🛑 STOP-Gate:**
- Wenn auch nur EIN SHA nicht matched → v4.1.4 ist NICHT shippable.
  Der Refactor hat eine Regression eingeführt. Stop, escalate,
  `docs/REFACTOR_NOTES.md` → "Hardware Truth Failure".
- Wenn RPM > ±0.5 driftet → gleicher Stop.
- Wenn `DriveAbsent` als `ProviderError` rauskommt → Rule F-4 verletzt,
  Stop.

---

## PHASE 4 — Tag + Push (irreversibel, explizite Freigabe pro Step)

**Pre-condition:** Phase 1+2+3 ALLE grün. `uft_p24_verify.sh` muss
0 Fails liefern.

### Step 4.1 — CHANGELOG-Header finalisieren
- [ ] `## [4.1.4-rc1] - 2026-05 (pre-release)` → `## [4.1.4] - <YYYY-MM-DD>`
- [ ] Diff seit RC1 einklappen: `git log --pretty='- %s (%h)' v4.1.4-rc1..HEAD`
      → MF-236..MF-242 + Phase-1-Commits in Added/Changed/Fixed kategorisieren

### Step 4.2 — ⚠ Tag setzen
- [ ] Nach Phase-2-Entscheidung: `git tag -s v4.1.4 …` ODER
      `git tag -a v4.1.4 …` (siehe Phase 2).
- [ ] **WICHTIG:** Tag-Name ohne Hyphen → `release.yml`'s
      `prerelease: contains(github.ref_name, '-')` evaluates false →
      GitHub marked als "Latest" (MF-181).
- [ ] `git tag -v v4.1.4` (signed) oder `git show v4.1.4` (unsigned)
      verifizieren.

### Step 4.3 — ⚠ Push (Point of no return)
**🛑 EXPLIZITE GO/NO-GO-ABFRAGE AN AXEL VOR PUSH.**
- [ ] `git push origin main` (falls Phase-1-Commits noch nicht gepusht)
- [ ] `git push origin v4.1.4`
- [ ] `gh run watch` → release-Workflow muss `completed/success` auf allen
      Plattform-Jobs erreichen.

**Bei Build-Fail nach Tag-Push:** Tag ist verbrannt. Forward-fix als
`v4.1.4.1` oder `v4.1.5`. **NIEMALS** den Tag force-moven.

### Step 4.4 — Release verifizieren
- [ ] `gh release view v4.1.4` — existiert, "Latest", nicht pre-release
- [ ] Artefakte: `.deb`, `.tar.gz` (Linux), `.dmg`, `.tar.gz` (Windows),
      `SHA256SUMS` — identische Set wie v4.1.0+

### Step 4.5 — Post-Release
- [ ] `releases/v4.1.4/hil_report.md` mit signed-off Resultat archivieren
- [ ] `docs/REFACTOR_TASKS.md` P2.3 + P2.4 → ✅ mit v4.1.4-Tag-Hash
- [ ] Backlog-Tasks (M3.x, ARCH-7, ARCH-8, ARCH-9, §1.2, §6.1, Plugin-
      Compliance) in v4.1.5- bzw. v4.3-Milestone moven

---

## STOP-Conditions (übernommen aus REFACTOR_TASKS.md, erweitert)

Halt sofort und frage Axel wenn:
1. Phase 1 Verifikation zeigt ein neuer stale-Claim, der nicht in den
   4 Befunden enthalten ist.
2. `git diff main origin/refactor/type-driven-hal -- src/ include/`
   ist nicht leer und nicht offensichtlich erwartete
   Hardening-Differenz.
3. HIL Phase 3 zeigt SHA-Mismatch, RPM-Drift > ±0.5 oder F-4-Verletzung.
4. PR #26 git-notes-Erklärung passt nicht zur Wahrheit (z.B. wenn das
   tatsächlich kein Tippfehler war).
5. CI auf main (post-Phase-1) ist auf einer der 4 Plattformen (Linux GCC,
   Linux Qt-Matrix, macOS Clang, Windows MinGW) rot.
6. Ein neuer `MF-`-Commit auf main landet während des Goals, der nicht
   geplant war.

In allen Fällen: aktueller State mit `WIP:`-Prefix committen, Stop-Grund
in `docs/REFACTOR_NOTES.md` festhalten, auf Axel warten.

---

## Per-Task-Contract (übernommen aus REFACTOR_TASKS.md)

Jeder Task endet mit allen folgenden true:

- [ ] `cmake --build build` ODER `qmake && mingw32-make` grün
- [ ] `ctest --output-on-failure` grün; Konformanz-Test-Count ≥ Baseline
- [ ] `python scripts/check_consistency.py` 0/0/0/0
- [ ] `python scripts/verify_build_sources.py` no new divergence
- [ ] Commit-Message folgt Conventional Commits + trägt MF-NNN
- [ ] `bash uft_p24_verify.sh` zeigt für die zugehörige Section ✓

**Verify-Skript:** `uft_p24_verify.sh` (im Repo-Root oder am Pfad den Axel
nennt). Läuft Section 0-6 durch und zeigt für jeden der 4 Befunde ob er
behoben ist. Erwartet am Ende von Phase 1: 3 Fails behoben (RELEASE_NOTES
×3); BEFUND 4 (HIL) bleibt bis Phase 3.

---

## Rollback / Abort

- **Vor Phase 4.3 (Push):** alles ist lokal. `git reset`, re-do, oder
  abandon. Kein public impact.
- **Nach Phase 4.3:** Tag + Release sind public. **Niemals** Tag löschen
  oder moven. Forward-fix mit `v4.1.4.1` oder `v4.1.5`. Einzige Ausnahme
  (Release-pulling) ist separat zu autorisieren, nicht Teil dieses Goals.

---

## Reproduce / Pre-Flight

```bash
# Read-only — jederzeit ausführbar:
bash uft_p24_verify.sh
gh run list --branch main --limit 5
cat VERSION.txt && python scripts/check_consistency.py
git rev-list --left-right --count main...origin/refactor/type-driven-hal
git log --pretty='- %s (%h)' v4.1.4-rc1..HEAD
```

---

**Ende des Goals.** Bei Abschluss aller 4 Phasen: Tag `v4.1.4` ist live,
GitHub-Release ist "Latest", `REFACTOR_TASKS.md` P2.3+P2.4 sind ✅,
v4.1.5-Milestone ist mit dem ehrlichen Backlog gefüllt.
