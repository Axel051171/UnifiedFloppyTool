#ifndef UFT_FORMATS_UFT_MSA_H_FWD
#define UFT_FORMATS_UFT_MSA_H_FWD
/* Forward-include: canonical header is uft/uft_msa.h
 *
 * MF-424: this file used to carry its own 130-line copy of the same
 * declarations. Two copies drift; that is where FMT-14, PROT-12,
 * ARCH-1 and ARCH-2 all came from. No translation unit included this
 * path, so nothing changes for any caller -- the duplication is
 * simply gone, and the include path stays valid. */
#include "uft/uft_msa.h"
#endif /* UFT_FORMATS_UFT_MSA_H_FWD */
