#ifndef UFT_CORE_UFT_INTEGRATION_H_FWD
#define UFT_CORE_UFT_INTEGRATION_H_FWD
/* Forward-include: canonical header is uft/uft_integration.h
 *
 * MF-424: this file used to carry its own 182-line copy of the same
 * declarations. Two copies drift; that is where FMT-14, PROT-12,
 * ARCH-1 and ARCH-2 all came from. No translation unit included this
 * path, so nothing changes for any caller -- the duplication is
 * simply gone, and the include path stays valid. */
#include "uft/uft_integration.h"
#endif /* UFT_CORE_UFT_INTEGRATION_H_FWD */
