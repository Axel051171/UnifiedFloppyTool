#ifndef UFT_FDI_H_FWD
#define UFT_FDI_H_FWD
/* Forward-include: canonical header is uft/formats/uft_fdi.h
 *
 * MF-425: this file carried a byte-for-byte second copy of the same 333
 * declarations. No translation unit included either path, so nothing
 * changes for any caller.
 *
 * NOTE ON THE NAME: this FDI is 'FAT Disk Image'. UFT also has an
 * unrelated FDI *format plugin* (src/formats/fdi/) for the ZX-Spectrum
 * FDI container, whose corpus entry is zxart_spectrofon01.fdi. Two
 * different things share the abbreviation; do not merge them. */
#include "uft/formats/uft_fdi.h"
#endif /* UFT_FDI_H_FWD */
